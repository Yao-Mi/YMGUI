# YMGUI 代码风格规范

> **本文档定位**：YMGUI 是一个**面向嵌入式/裸机优先**的跨平台 GUI 库（软件光栅化 + 保留模式 + 分块刷新）。它的工程约定**大量继承自 YMCV**（同一作者、同一"可移植到嵌入式"第一约束），凡是 YMCV 从这个约束倒推出来的东西——内存双档、生命周期成对、入口三连校验、LUT 优先、数学宏、裁剪开关——YMGUI 原样沿用，不重新发明。
>
> **与 YMCV 的三点根本差异（本文档专门处理的部分）**：
> 1. **有状态 / 交互**：YMCV 是无状态变换库（图进→算法→图出）；YMGUI 是有状态交互库（事件→改状态→增量重绘）。多出 `GUI/` 层：失效/脏矩形、事件分发、焦点、布局。
> 2. **树形所有权**：YMCV 对象扁平（谁申请谁释放）；YMGUI 是对象树，`Free` 一个容器要**级联释放子节点**。
> 3. **HAL 最窄腰部**：整库收敛到一个显示驱动回调 `flush(area, buf)` + 输入注入，平台适配只动 `HAL/` 的实现（`SDL_LCD/` 等）。
>
> **核心原则**：新代码要"读起来像 YMCV 的门面代码"——把 YMCV 里最好的那部分固化成规矩。前缀从 `CV`/`YMCV_` 平移到 `GY`/`YMGUI_`。
>
> 配套：仓库根目录 `.clang-format` 直接复用 YMCV 的配置（Tab/Allman/指针左靠），新文件写完 `clang-format -i` 即可。

---

# A. 命名规则

## 1. 类型与变量（前缀 CV→GY 平移）

| 类别 | 规则 | YMGUI 示例 | 对应 YMCV |
|------|------|-----------|-----------|
| 平台通用数据类型 | **全小写** | `int8` `uint16` `uint32` `float32` | 同（继承 `Pubtype.h`） |
| 结构体类型 | `GY` + 小写 | `GYobj` `GYrect` `GYsurface` `GYdisp` | `CVimage` |
| 结构体指针类型 | `GY` + **全大写** | `GYOBJ` `GYSURFACE` `GYDISP` | `CVIMAGE` |
| 枚举类型 | `GY` + 驼峰 | `GYColorDepth` `GYObjType` `GYEvent` | `CVImgType` |
| 枚举成员 | `GY_` + 驼峰/下划线 | `GY_EVENT_Pressed` `GY_LOG_PtrI` | `CV_ThreshToBin` |
| 内存函数 | `GY_` 前缀 | `GY_malloc0` `GY_malloc1` `GY_free0` | `CV_malloc0` |
| 调试宏 | **小写** `gy_` 前缀 | `gy_assert` `gy_log_explain` | `cv_assert` |
| 局部变量 | 小驼峰或下划线均可 | `dirty_area` `objp` `pxout` | 同 |

**值类型小写 + 指针 typedef 全大写成对出现**（`GYobj`/`GYOBJ`、`GYsurface`/`GYSURFACE`），全库贯彻，API 里几乎只出现大写指针别名——与 YMCV 完全一致。

## 2. 函数命名（三档，与 YMCV 同规矩）

| 函数类型 | 命名 | 链接性 | 示例 |
|---------|------|--------|------|
| **对外 API** | `YMGUI_` + 首字母大写单词，下划线分段 | extern | `YMGUI_Obj_Invalidate` `YMGUI_Draw_Fill` |
| **文件内私有辅助** | **小驼峰，无前缀** | **必须 `static`** | `mergeDirtyRect` `clipToSurface` |
| 跨文件共享但非公开 | 加 `YMGUI_`/`GY_` 前缀 | extern | `GY_Rect_Intersect` |

**关键约束**（照抄 YMCV §1.2）：小驼峰无前缀的前提是 `static`（锁在本文件），否则污染全局命名空间。一旦要被别的文件调用，就必须加前缀提升到第三档。

- 类型名用 `GY` 前缀，对外函数用 `YMGUI_` 前缀，两者不混。

## 3. 对外 API 的动词短语约定（GUI 专属补充）

GUI 是交互库，API 语义比 YMCV 的"变换"丰富，建议按动作归类命名，读起来自解释：

| 语义类别 | 动词 | 示例 |
|---------|------|------|
| 创建/销毁（生命周期） | `Creat` / `Free` | `YMGUI_Creat_Button_Creat` / `YMGUI_Free_ObjFree` |
| 标脏/触发重绘 | `Invalidate` | `YMGUI_Obj_Invalidate` `YMGUI_Obj_InvalidateArea` |
| 事件注入（拉取式输入） | `Inject` | `YMGUI_Inject_Pointer` `YMGUI_Inject_Key` |
| 刷新一帧（核心驱动） | `Refresh` | `YMGUI_Refresh` |
| 底层图元绘制 | `Draw` | `YMGUI_Draw_Fill` `YMGUI_Draw_Glyph` `YMGUI_Draw_Img` |
| 属性设置/读取 | `Set` / `Get` | `YMGUI_Obj_SetPos` `YMGUI_Label_SetText` |

---

# B. 目录结构与"新文件放哪"

## 1. 目录命名刻意对齐 YMCV（移植直觉可整体迁移）

用户记住的 YMCV 移植故事是:**拷贝库目录 → 只改 `CONFIG/` → 换掉 `SDL_LCD/`**。YMGUI 让**角色相同的层用一字不差的名字**,GUI 独有的层用同样的全大写风格新增,于是同一套移植直觉直接适用。

| YMCV 目录 | YMGUI 目录 | 角色 | 移植时 |
|-----------|-----------|------|--------|
| `CONFIG/` | `CONFIG/` | 平台适配(Mem/PubDefine/PubType) | **主战场,只改这层** |
| `DEBUG/` | `DEBUG/` | assert/log 横切 | 一般不动 |
| `COMMON/` | `COMMON/` | 平台无关纯算法(不碰核心像素类型) | 不动 |
| `OPOBJ/` | `OPOBJ/` | Creat/Free/OBJ 生命周期 | 不动 |
| `CORE/` | `CORE/` | **碰核心像素类型**的层(YMCV=CVimage,YMGUI=GYsurface 光栅化) | 不动 |
| `IOFILE/` | `IOFILE/` | 文件/字体文件读写 | 按文件系统改宏 |
| `SDL_LCD/` | `SDL_LCD/` | **Linux 假 LCD 实现**(桌面开发用) | **换成真实 LCD 驱动** |
| `Demo/` | `Demo/` | 示例 | — |
| （无对应） | `HAL/` | 显示驱动 + 输入注入**纯接口**(最窄腰部) | 定义契约,不动 |
| （无对应） | `GUI/` | ★GUI 机器(失效·脏矩形·事件·焦点·布局) | 不动 |
| （无对应） | `WIDGET/` | button/label/textinput/list... | 不动 |

**关键**：YMCV 里 `SDL_LCD/` 直接实现画点/画线/显图；YMGUI 里 `SDL_LCD/` 只干一件事——实现 `HAL/` 定义的 `flush_cb`,把软件渲染好的 framebuffer 推给 SDL 纹理"假装成一块 LCD"。移植到真实硬件时,用户照着 `SDL_LCD/` 依葫芦画瓢写一个 SPI/并口版 `flush_cb` 即可,和 YMCV 换 LCD 驱动是同一个心智动作。

## 2. 分层依赖（严格单向，自底向上）

```
CONFIG   ← 平台适配 + 编译期配置(COLOR_DEPTH / coord 宽度 / buffer 尺寸 / 数学宏 / 裁剪开关)
   ↑
HAL      ← 显示驱动接口 + 输入注入接口(纯头,最窄腰部 flush(area,buf))
   ↑
COMMON   ← 平台无关纯算法(几何 rect/point 运算、定点数、颜色转换),不碰 GYsurface
   ↑
OPOBJ    ← 对象生命周期(Creat/Free,统一内存出入口,★树级联)
   ↑
CORE     ← 软件光栅化层(碰 GYsurface/framebuffer 的全在这:fill/line/glyph/img/blend + 字体/渐变资源)
   ↑
GUI      ← ★GUI 机器(失效·脏矩形·事件分发·焦点·布局)—— YMCV 无对应,全新
   ↑
WIDGET   ← button/label/textinput/list...
   ↑
IOFILE / SDL_LCD  ← 可选外围(文件、字体文件) / HAL 的具体实现(Linux 假 LCD → 真实 LCD)
   ↑
DEBUG    ← 横切层(assert/log),被所有层调用
```

## 3. 新文件归位判据（把 YMCV §11 的"是否碰 CVimage"平移为"是否碰 GYsurface"）

YMCV 的判据是一句话:**是否操作 `CVimage`**——碰图像进 `CORE/`,不碰进 `COMMON/`。YMGUI 平移这条判据,并加一条 GUI 维度:

- **是否直接读写 `GYsurface`/framebuffer 像素** → 碰像素进 `CORE/`(光栅化、字形栅格、渐变采样都在此);不碰的纯几何/数据结构进 `COMMON/`。
- **是否是 GUI 交互机制**(失效/事件/焦点/布局/对象树) → 进 `GUI/`。
- 控件 → `WIDGET/`;平台实现 → `SDL_LCD/`(或用户自建的 LCD port 目录);字体/图片文件读取 → `IOFILE/`。

`COMMON/` 必须能脱离 GUI 单独复用(就像 YMCV 的 `COMMON/` 能脱离图像)。`HAL/` 只放纯接口,`SDL_LCD/` 放实现——这条缝保证"Linux 开发 / 裸机部署"零分叉。

`COMMON/` 建议沿用 YMCV 的子类划分(按需建):`COMMON/GEOM/`(几何/rect 运算)、`COMMON/MATH/`(定点数/FastMath)、`COMMON/STRUCT/`(链表/栈/脏矩形表结构)。

- 新增目录记得加进根 `CMakeLists.txt` 的 `include_directories` 和对应 `add_library`。

---

# C. 核心类型约定（GUI 专属，最重要）

## 1. 三层颜色分离（务必理解，别混用）

YMGUI framebuffer 是 RGB888/RGB565/灰度/1bpp（无 alpha 通道；RGB888 每像素 3 字节），但抗锯齿文字/半透明控件仍需 alpha。所以**三个概念严格分开**（对标 YMCV 的 `CVcolor`/`_color8_t` 但语义扩展）：

| 概念 | 类型 | 用途 | 存在于 |
|------|------|------|--------|
| **API 颜色** | `GYcolor`（32 位 ARGB） | 用户描述颜色 `0xFF3080C0`，只是描述不是存储 | 公开 API |
| **表面像素** | `GYpx`（编译期 = RGB888/RGB565/灰度/1bpp） | framebuffer 实际存储 | `CORE/` + `HAL/` |
| **覆盖度** | `GYopa`（8 位 alpha） | 混合那一瞬间用，不进 framebuffer | `CORE/` 混合内部 |

编译期由 `GYGUI_COLOR_DEPTH` 宏切换 `GYpx`（对标 YMCV 的 `LV_COLOR_DEPTH` 思路）。混合流程：读目标 `GYpx` → 升精度 → 用 `GYcolor` + `GYopa` 混合 → 打包回 `GYpx` 写入。

**副作用（必须记住）**：1bpp 单色屏无法抗锯齿（只能阈值化 on/off），灰度和 565 可以。字体渲染要按 `COLOR_DEPTH` 分支。

## 2. 数值类型：定点/整数为主（无 FPU 友好）

| 类型 | 定义 | 用途 |
|------|------|------|
| `GYcoord` | 编译期 `int16`（默认）或 `int32` | 几何/布局坐标，整数像素是工作单位 |
| `GYvalue` | `int32` 定点（16.16） | 子像素/动画插值/字体度量，配 `GY_FP(x)`/`GY_INT(fp)` 宏 |

**主几何保持整数，float 不出现在必经路径**——无 FPU 芯片不受罚。这是 YMCV §12 性能哲学的直接延续。数学宏走 `CONFIG/` 的 `YMGUI_Sqrt`/`YMGUI_Sin`（对标 `YMCV_Sqrt`），移植时改宏右边换定点/CMSIS-DSP。

## 3. 核心结构体范式（对标 CVimage 的裸字节 + 类型步进）

surface 不是"整屏"，而是"当前 band 在屏幕坐标系里的窗口"（分块刷新的关键）：

```c
//表面（光栅化器的绘制目标）
typedef struct
{
	GYpx*   buf;         //指向 disp 的 draw buffer
	GYrect  buf_area;    //这块 buffer 当前映射到屏幕的哪个矩形
	GYrect  clip;        //当前裁剪区(屏幕坐标)，控件绘制时收窄
	GYcoord stride;      //一行多少像素
}GYsurface;
typedef GYsurface* GYSURFACE;
```

**像素访问沿用 YMCV §10**：`buf` 是 `GYpx*`，绘制前把它按 `COLOR_DEPTH` 转成类型化指针再 `[i]` 步进，不直接裸操作。所有绘制函数收**屏幕坐标**，内部 clip 到 `surface.clip` 再平移到 `buf_area` 原点写入——图元完全不知道有没有 OS、是 SDL 还是真 LCD，**这是可移植性的锚点**。

---

# D. 三条横切主线（原样继承 YMCV）

## 1. 内存双档制（不直接 malloc/free）

来自 `CONFIG/`（对标 `CONFIG/YMCV_Mem.c`），移植时只改这里：

| 函数 | 用途（GUI 场景） |
|------|-----------------|
| `GY_malloc0(size)` / `GY_free0(ptr)` | **小/快**（高速内存 CCM/TCM）：对象头、样式结构、事件、脏矩形表、字形表、渐变 256 项 LUT |
| `GY_malloc1(size)` / `GY_free1(ptr)` | **大/慢**（低速大内存 SDRAM/PSRAM）：draw buffer、framebuffer、解码图片、字形位图 |
| `GY_realloc0/1` | 重置长度 |
| `GY_memset/GY_memcpy` | 内存设置/拷贝 |

规则：**大块数据用 `GY_malloc1`，小结构用 `GY_malloc0`**，谁申请谁负责用对应 `GY_freeN` 释放。GUI 的大小内存分界甚至比 YMCV 更清晰。

## 2. 对象生命周期（OPOBJ 模式 + 树级联，本处与 YMCV 有别）

`YMGUI_Creat_*` 与 `YMGUI_Free_*` 严格成对、镜像对称（对标 YMCV 的 `Creat.h`/`Free.h` 一一对应）。创建函数固定骨架：

```
GY_mallocX → gy_assert + gy_log_explain(...Mem0/Mem1...) → 填字段 → 返回
```

**GUI 专属修正——树级联**：YMCV 对象扁平（申请谁释放谁）；YMGUI 是对象树，`YMGUI_Free_ObjFree(parent)` 必须**递归释放所有子节点**再释放自己。Creat/Free 镜像成对原则保留，但语义上多一条"父析构级联子"。用扁平心智会漏 free 或重复 free——这是从 YMCV 迁过来最容易踩的坑。

## 3. 入口防御式校验（DEBUG 层，几乎每个函数入口）

来自 `DEBUG/`（对标 `YMCV_Debug.h`），错误码位掩码枚举 `GYEVNLOG`（`GY_LOG_PtrI=0x01`…`GY_LOG_Mem1=0x20`…可组合），整层由 `YMGUI_ASSERT_DEBUG`/`YMGUI_DEBUG_MODE` 宏开关，发布关掉：

```c
gy_assert(obj);                                              //为空则打印文件+行号
gy_log_explain(obj == NULL, GY_LOG_PtrI, "输入对象不存在");   //条件成立则输出中文提示
gy_log_explain(obj->type != GY_OBJ_Button, GY_LOG_TypeI, "对象类型非按钮");
```

**约定顺序**（照抄 YMCV §7）：先 `gy_assert` + `gy_log_explain` 校验输入/输出指针，再校验类型，再校验参数，最后干活。

---

# E. HAL 契约与输入注入（GUI 专属，最窄腰部）

整库收敛到 `HAL/` 一个纯头接口，`SDL_LCD/` 等各自实现（对标 YMCV 的 `SDL_LCD/` 但抽象成回调）：

```c
typedef struct GYdisp
{
	GYcoord hor_res, ver_res;        //屏幕尺寸(整数像素)
	GYpx*   buf1;                    //draw buffer，可远小于整屏
	GYpx*   buf2;                    //可选第二块，flush DMA 时双缓冲重叠
	uint32  buf_px_cnt;              //每块能放多少像素 → 决定 band 高度
	//硬件唯一必须实现的：把 buf 里 area 大小的一块连续像素推到面板
	void (*flush_cb)(struct GYdisp* d, const GYrect* area, const GYpx* buf);
	void*   user_data;               //存 SPI 句柄 / SDL_Texture 等
}GYdisp;
typedef GYdisp* GYDISP;
```

- **画点不作必选**：它只是 flush 一个 1×1 area 的退化情况，绝大多数面板支持窗口 blit。
- **输入走注入**（拉取式，裸机无 OS 消息泵）：`YMGUI_Inject_Pointer(ctx, x, y, pressed)` / `YMGUI_Inject_Key(...)`。SDL port 把 `SDL_Event` 翻译成注入调用；裸机 port 把触摸控制器读数翻译成注入调用。**库核心对"事件哪来的"一无所知**。

---

# F. 渲染器四段式：只用于"绘制资源"，不用于"控件树"

YMCV 的灵魂是 `Creat → Load → Render → Free`（预计算与执行分离，Load 算一次核/表，Render 多帧复用）。**这个价值在 YMGUI 里只存在于"绘制资源"，绝不套给控件树。**

## 1. 能套四段式的（照搬，是 LUT 哲学的自然延续）

- **字体**：`Creat`(选字体/字号) → `Load`(栅格化进 glyph cache/建字形表) → `Render`(反复 blit 文字) → `Free`。
- **渐变**：`Creat`(线性/径向) → `Load`(预计算 256 项颜色 LUT) → `Render`(填充采样 LUT)。
- **图片/精灵**：`Load` 时解码 + 转 native 像素格式一次 → `Render` 反复 blit。
- **565 混合表**：预计算混合 LUT。

落到设计就是**绘制描述符 `draw_dsc`**：配置一次怎么画（颜色/线宽/字体/渐变），发很多次绘制调用。这是 YMCV 渲染器在 GUI 里的自然归宿。

## 2. 不能套四段式的（会翻车）

控件不是"被 Render 的一帧流"，它是**持久对象**，只在被 invalidate 时重绘。生命周期是 `创建控件 →（事件改状态→标脏→核心重绘脏区）→ 销毁`。这里的"重绘"由**脏矩形系统驱动**，不是用户循环调 `Render(frame)`。硬把控件塞进四段式，会发现没地方放持久交互状态和重绘触发——因为 YMCV 从没这个概念。

**一句话：四段式搬给"资源和图元"，别搬给"控件和树"。**

---

# G. 失效/脏矩形 + 渲染管线（GUI 发动机，YMCV 无对应）

保留模式 + 分块刷新的核心。约定：

- 控件状态变 → `YMGUI_Obj_Invalidate(obj)` → 向上传播失效 → 收集脏区域。
- 一帧 `YMGUI_Refresh(ctx)`：合并脏矩形 → 按 `buf_px_cnt` 切成若干 band → 逐 band 重绘相交控件写进 draw buffer → `flush_cb` 推到面板（唯一碰硬件处）→ buf2 存在则 DMA 异步重叠下一条 band。
- **RAM 只够 40 行照样跑**（band < 屏幕），**够整屏则 band == 整屏一次 flush**。同一套代码覆盖 MCU 到桌面全谱系——这是分块刷新做地基的价值。
- **即时逃生口的代价**：自绘区域被标记为永远脏、每帧重画，写这类控件时心里有数。

---

# H. 格式规范（复用 YMCV 的 .clang-format）

| 维度 | 规则 |
|------|------|
| 缩进 | 库用 **Tab**（宽度按 4 显示）；Demo 层用 4 空格 |
| 大括号 | **Allman**：`{` 另起一行 |
| 指针 | **左靠类型** `GYpx* p` |
| 控制语句 | `if (` `for (` 括号前留空格 |
| `switch`/`case` | `case` 与 `switch` 同列 |
| 行宽 | 不强制（保留行尾中文注释） |
| `#include` | 裸头式 `#include "YMGUI_Xxx.h"`（不带路径，全库头文件无重名），不自动重排 |
| 编码 | **UTF-8 / LF**，注释用中文 |
| 数值常量 | 带类型后缀，浮点用 `f`（`1.0f`） |

新文件写完 `clang-format -i 文件.c`（配置直接复用 YMCV 根目录 `.clang-format`）。

---

# I. 文件头 banner 与函数注释（沿用 YMCV 模板）

每个 `.c` 在 `#include` 之后放标准 banner，**逐字段替换**，保留 YAOMI TECH ASCII art（同一作者、同一品牌）：

```c
/**
  ***************************************************************************************************************************
  *	@FileName:    YMGUI_Xxx.c
  *	@Author:      yaomimaoren
  *	@Date:        2026-07-01
  *	@Description: 一句话说明本文件功能
  *	@Version:     1.0
  *
  ***************************************************************************************************************************
  *
  * 备注信息：
  * 1.补充说明
  *
  ***************************************************************************************************************************
  * <author> <time> <version > <desc>
  * yaomi 26/07/01 1.0 build this moudle
  *
  *   __  __ ___    ____   __  ___ ____     ______ ______ ______ __  __
  *   \ \/ //   |  / __ \ /  |/  //  _/    /_  __// ____// ____// / / /
  *    \  // /| | / / / // /|_/ / / /       / /  / __/  / /    / /_/ /
  *    / // ___ |/ /_/ // /  / /_/ /       / /  / /___ / /___ / __  /
  *   /_//_/  |_|\____//_/  /_//___/      /_/  /_____/ \____//_/ /_/
  *
  * Copyright (C), 2026-2036, YAOMI Tech. Co., Ltd.
  ***************************************************************************************************************************/
```

banner 内部 **2 空格 + `*`** 对齐。对外函数上方用 `@brief` 块注释（`*` 前 2 空格）；函数体内用**中文行注释** `//` 紧贴代码，涉及公式把公式写进注释——全部与 YMCV §3/§4 一致。

---

# J. 头文件规范（沿用 YMCV §5/§6）

- 守卫用 `#ifndef / #define / #endif`，宏名文件名大写 `YMGUI_XXX_H`，**不带前导下划线**。
- 结尾 `#endif` 带注释 `// !宏名`。
- 头内顺序：守卫 → `#include` 依赖 → `typedef enum` → `typedef struct` + 指针 typedef → 函数声明。
- 函数声明后跟一句**中文行注释**说明用途。
- **裸头式 include**：`#include "YMGUI_Xxx.h"` 不带路径，CMake 把每个含头目录加进搜索路径，文件挪位置不断链。顺序：先本模块头，再依赖的 `DEBUG`/`CONFIG`/`GUI` 头（人工排序，不自动重排）。

---

# K. 性能与优化惯用手法（继承 YMCV §12，GUI 场景落地）

性能哲学不变：**面向嵌入式（无 FPU、内存紧），空间换时间、整数/移位换浮点、位打包省内存**。GUI 场景的具体落点：

1. **查表 LUT（核心手段）**：渐变预算 256 项颜色表、565 alpha 混合表、字形 cache、`GYGUI_Sin360[361]` 代 `sinf`（圆弧/旋转控件用）。
2. **类型化指针访问**：`buf` 按 `COLOR_DEPTH` 转 `GYpx` 类型指针再步进——RGB565 每像素 2 字节，**必须**转对否则取错（正确性要求，见 §C.3）。
3. **位打包 1bpp**：单色 framebuffer 用 `GYGetBin/GYSetBin/GYReSetBin`（对标 YMCV 的 `CVGetBin`，`index>>3` 定位字节、`1<<(index&7)` 定位 bit），省 8 倍内存。你上一轮选了单色屏，这段几乎可从 YMCV 直接拿。
4. **脏矩形省 blit**：LCD 的 blit 是最贵操作，保留模式只重绘变化区域把传输降到最低——这是 GUI 独有的省法。
5. **移位代乘除**：`>>8` 近似 `/256` 等定点手法。

**SIMD 认知同 YMCV §12**：LUT 查表是数据相关 gather，编译器通常不自动向量化；规整逐像素核（如常量填充、alpha 直算）开 `-O3` 才吃自动 SIMD。裸机（Cortex-M）DSP-SIMD 要手动/CMSIS-DSP。SDL/桌面 port 可选叠加，不作默认要求。

---

# L. 其他约定（对标 YMCV §10）

- 语言 **C99 + GNU 扩展**，注释中文，UTF-8/LF。
- 模块可裁剪：`CONFIG/` 里 `YMGUI_XXX_USE` 开关，功能代码 `#if YMGUI_WIDGET_LIST_USE ... #endif` 包裹，按 Flash/RAM 预算砍。
- 数学/常量走 `CONFIG/` 封装宏（`YMGUI_Sqrt`/`YMGUI_Sin`/`YMGUI_Pai`/`GYMax`/`GYMin`/`GYLimitMaxMin`），别直接调 libm。
- 数值常量带类型后缀，浮点 `f`。

---

# 附：新建一个模块的最小骨架

## CORE/ 图元模块（碰 surface）

`CORE/YMGUI_DrawFill.h`
```c
#ifndef YMGUI_DRAWFILL_H
#define YMGUI_DRAWFILL_H
#include "YMGUI_PubDefine.h"
#include "YMGUI_PubType.h"
#include "YMGUI_Surface.h"

void YMGUI_Draw_Fill(GYSURFACE s, const GYrect* area, GYcolor color, GYopa opa);//在表面填充矩形区域

#endif // !YMGUI_DRAWFILL_H
```

`CORE/YMGUI_DrawFill.c`
```c
#include "YMGUI_DrawFill.h"
#include "YMGUI_Debug.h"
#include "YMGUI_Mem.h"

/** banner 见 §I */

/**
  * @brief 在表面填充矩形区域（clip 到 surface.clip 后写入 buffer）
  */
void YMGUI_Draw_Fill(GYSURFACE s, const GYrect* area, GYcolor color, GYopa opa)
{
	GYrect draw_area;
	GYpx*  bufp;
	GYcoord x, y;
	//输入指针判断
	gy_assert(s && area);
	gy_log_explain((s == NULL) || (area == NULL), GY_LOG_PtrI, "表面或区域不存在");
	gy_assert(s->buf);
	gy_log_explain(s->buf == NULL, GY_LOG_PtrI, "表面缓冲区不存在");

	//区域裁剪：填充区 ∩ 表面裁剪区
	if (GY_Rect_Intersect(&draw_area, area, &s->clip) == 0)
		return;//完全在裁剪区外，无需绘制

	//转类型化指针（不直接裸操作 s->buf）
	bufp = (GYpx*)s->buf;
	//这里按 opa 分支：不透明→直写打包色；半透明→读目标 GYpx 混合再写回（见 §C.1）
	for (y = draw_area.y; y < draw_area.y + draw_area.h; y++)
	{
		for (x = draw_area.x; x < draw_area.x + draw_area.w; x++)
		{
			//屏幕坐标 → buffer 偏移：减去 buf_area 原点
			GYcoord bx = x - s->buf_area.x;
			GYcoord by = y - s->buf_area.y;
			bufp[by * s->stride + bx] = GY_ColorToPx(color);//opa==255 分支
		}
	}
}
```

## OPOBJ/ 生命周期成对（带树级联）

```c
GYOBJ btn = YMGUI_Creat_Button_Creat(parent, w, h);   //创建，挂到 parent 子链
...
YMGUI_Free_ObjFree(btn);                              //释放：递归 free 子节点 → free 自己
```

创建函数标准写法：`GY_mallocX` → `gy_assert` + `gy_log_explain(...Mem0...)` → 填字段 + 挂父子链 → 返回。释放函数：先遍历子链递归 `YMGUI_Free_ObjFree` → 从父链摘除 → `GY_freeN`。

---

# M. 与 YMCV 的对照速查

| YMCV | YMGUI | 关系 |
|------|-------|------|
| 目录 `CONFIG/DEBUG/COMMON/OPOBJ/CORE/IOFILE/SDL_LCD` | 同名 `CONFIG/DEBUG/COMMON/OPOBJ/CORE/IOFILE/SDL_LCD` | **一字不差**,移植直觉迁移 |
| `CV`/`CVIMAGE` | `GY`/`GYOBJ` | 前缀平移 |
| `YMCV_` | `YMGUI_` | 前缀平移 |
| `cv_assert`/`cv_log_explain` | `gy_assert`/`gy_log_explain` | 直接搬 |
| `CV_malloc0/1` | `GY_malloc0/1` | 直接搬（双档制） |
| `CVimage`(type+sizet+裸data) | `GYsurface`(COLOR_DEPTH+buf) | 泛型化思路搬 |
| `Creat/Free` 成对 | `Creat/Free` 成对 **+ 树级联** | 搬 + 修正 |
| 渲染器四段式 | `draw_dsc` 资源四段式 | 只搬给资源 |
| `CVGetBin/CVSetBin` | `GYGetBin/GYSetBin` | 直接搬 |
| `YMCV_Sqrt/Sin` 宏 | `YMGUI_Sqrt/Sin` 宏 | 直接搬 |
| `YMCV_XXX_USE` 裁剪 | `YMGUI_XXX_USE` 裁剪 | 直接搬 |
| （无） | `GUI/` 失效·事件·焦点·布局 | GUI 全新 |
| `SDL_LCD/` 直接函数 | `HAL/` flush 回调 + `SDL_LCD/` 实现 | 抽象成腰部 |
