# YMGUI 仓库维护约定

## 默认阅读顺序

1. 先读 `README.md` 和 `docs/当前状态.md`。
2. 根据任务按需读 `docs/API.md`、`docs/ARCHITECTURE.md`、`docs/CROSS_PLATFORM_PORTING.md` 或 `docs/代码风格.md`。
3. `docs/DEVLOG_V2.md` 是当前续写的开发进程文档；需要承接上轮背景或本轮完成后追加记录时阅读。
4. `docs/history/` 是冻结的历史归档。除非需要追溯决策、排查回归或用户明确询问历史，不要读取或修改。

## 事实优先级

当说明与实现冲突时，依次以代码/测试/CMake、`docs/当前状态.md`、当前专题文档、`docs/DEVLOG_V2.md`、历史归档为准。DEVLOG 中的计数和方案表示对应开发轮次的状态。

## 文档维护

- `docs/当前状态.md` 是所有 DEVLOG 最终成果的去时间线汇总，必须列全已完成功能和当前边界，不能只保留计数或入口摘要。
- 功能、构建方式、目录、配置、测试数量或关键约束变化时，在续写 DEVLOG V2 的同时同步更新 `docs/当前状态.md`。
- 公开 API 变化时更新 `docs/API.md`；架构、移植或规范变化时更新对应专题文档。
- 一轮实质开发工作完成后，按 V1 的记录方式将背景、改动、取舍、问题和验证续写到 `docs/DEVLOG_V2.md`。
- `README.md` 保持为面向使用者的简短入口，不再堆叠阶段开发日志。
- DEVLOG 记录演进过程，`当前状态.md` 记录当前结果；两者职责不同，但关键结论可以互相引用。
