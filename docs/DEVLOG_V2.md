# YMGUI 开发进程文档 V2

V2 是 [V1](history/DEVLOG_V1.md) 的直接续集，从第 42 轮开始持续记录开发过程、技术取舍、问题和验证结果。本文与 V1 保持相同定位，是项目的连续“来时路”，不是压缩版 Changelog。

当前事实仍以 [当前状态](当前状态.md)、代码、测试和 CMake 为准；V2 记录一项工作是如何演进到该状态的。V1 已归档，除追溯旧决策或排查回归外不需默认读取。

---

## 第 42 轮：RGB888 framebuffer 与双格式构建

- 新增 `YMGUI_COLOR_DEPTH=24`：`GYpx` 为 packed 的 3 字节 RGB888（内存顺序 R、G、B），补齐颜色转换、alpha 混合、图片 colorkey、SDL `RGB24` 输出。
- 保留并验证 `YMGUI_COLOR_DEPTH=16` 的 RGB565 路径；像素比较统一使用 `GY_PxEqual`，清零使用 `GY_PX_ZERO`，避免把 RGB888 结构体当整数访问。
- 顶层工程和 `project_Demo` 共用格式选项，构建目录按格式归档：`build/rgb565/...`、`build/rgb888/...`。
- `video_player` 按格式选择 FFmpeg `rgb565le` 或 `rgb24` 原始帧输入。
- 验证结果：顶层 RGB565/RGB888 各 35 项测试全部通过；10 个 `project_Demo` 在两种格式下均构建通过。
- **本轮补充：窗口关闭请求回调**。`SDL_LCD_SetCloseRequestCb(cb, user)` 仅属于桌面 SDL port；收到 `SDL_QUIT` 时，回调返回 0 取消本次关闭，返回非 0 才使 `SDL_LCD_PumpEvents()` 返回 0。未注册时保持旧行为，`Esc` 快捷退出不经过该回调。新增 `demo_close_request`（第一次关闭取消，第二次允许）和 `test_sdl_context` 允许/取消回归覆盖。

---

## 第 43 轮：GB2312 字模入库 + 文档分层与换卷

**GB2312 字模入库**：用户反馈不知道如何生成 `gb2312_glyphs.bin`，进一步希望直接下载该文件。原仓库在 `.gitignore` 中排除了 blob，只保留生成脚本和码点索引。现移除该排除规则，将 `Demo/gb2312_glyphs.bin` 正式纳入版本管理，普通使用者不再需要先安装 Pillow/Noto CJK 再生成。生成能力仍保留，用于修改字体、字集或光栅化参数。

**字库现状核对**：重跑 `Demo/gen_font.py --cjk --gb2312 --extern --bin` 并与入库 blob 逐字节比较。当前字集不是旧文档所说的 6763 个纯汉字，而是 GB2312 符号/标点区、一二级汉字及额外 `—–·` 共 7448 字形。16×16、4bpp，每字 128 字节，blob 为 953344 字节，SHA-256 为 `c2c42b2c1c2041c158a13b162549a03504202887d8179f476f9ef1a152d51316`。生成脚本帮助、配置头和 Demo 注释中的旧数字同步修正。

**文档分层**：根目录只保留面向使用者的 `README.md` 和维护规则 `AGENTS.md`。API、架构、跨平台移植和代码风格迁入 `docs/`；原 `PROJECT.md` 作为旧项目快照迁入 `docs/history/`；新建 `docs/当前状态.md` 作为当前事实的权威入口。维护者默认先读当前状态，再按任务读专题文档，不因为日常工作重读整卷旧日志。

**当前状态汇总**：用户进一步明确 `当前状态.md` 应当是“精简所有 DEVLOG”，而不只是入口摘要。因此按当前实现重写为完整功能总表：汇总显示/渲染、对象树、输入、内存与裁减、图元/字体、27 个具名控件、状态绑定、插件、平台适配和 10 个完整应用，并单列当前边界。今后“现在有什么”只查当前状态，“当时为什么这样做”才查 DEVLOG。

**DEVLOG 换卷**：用户明确 DEVLOG 仍是连续的项目开发记录，不应被 Git 历史完全取代。因此将第 0～41 轮归档为 `docs/history/DEVLOG_V1.md`，并在 `docs/DEVLOG_V2.md` 从第 42 轮继续。V1 的价值是保留历史上下文，V2 的价值是承接后续过程；`当前状态.md` 则避免日常工作必须从长日志里推断现状。

**验证**：GB2312 blob 可重现生成且与入库文件完全一致；Markdown 本地链接检查通过；根工程完整构建通过，**ctest 35/35**；`git diff --check` 通过。

---

## 第 44 轮：工具目录归位与自动化验证入口

**生成工具和产物归位**：新增仓库级 `tools/`，将字符/字体生成器从 `Demo/gen_font.py` 迁到 `tools/gen_font.py`，将已入库的外部字模从 `Demo/gb2312_glyphs.bin` 迁到 `tools/gb2312_glyphs.bin`。`Demo/` 继续只承担示例程序，生成器、可再生产物和维护脚本归入工具目录；三角函数表生成器仍留在 `Demo/`，因为本轮只调整字符生成链路。顶层 CMake、8 个使用中文回退字库的完整应用、源码注释和当前文档中的资源路径全部同步更新。V1 及第 43 轮中的旧路径保留为当时事实。

**字库验证自动化**：新增 `tools/verify_gb2312.sh`。快速模式检查入库 blob 的字形数、字节数和 SHA-256；`--rebuild` 模式调用 Pillow 和 Noto Sans CJK，在临时目录重新生成 blob 与 `YMGUI_FontDataGB2312.c`，再分别逐字节比较。这样既能尽早发现资源损坏，也能验证生成器、字形顺序和索引没有漂移。

**仓库级自动化入口**：新增 `tools/check_docs.py` 检查 Markdown 本地链接；`tools/check_repo.sh` 汇总链接、blob、`git diff --check`、关键数量和过期路径检查；`tools/test.sh` 配置、构建并测试一个色深；`tools/test_matrix.sh` 默认覆盖 RGB565/RGB888，`--all-depths` 可覆盖 1/8/16/24bpp。构建并发默认封顶为 8，可通过 `YMGUI_TEST_JOBS` 覆盖，避免高核数但资源受限的环境拉起过多编译任务。用法集中写入 `tools/README.md`，根 README 只保留常用入口。

**验证**：Python 脚本编译检查与 Shell 语法检查通过；Markdown 32 个本地链接全部有效；仓库计数和旧路径检查通过；7448 字形、953344 字节的 GB2312 blob 重生成后与入库文件一致；RGB565、RGB888 根工程均完整构建，两个配置各 **ctest 35/35**；`SDL_VIDEODRIVER=dummy` 下 `demo_font_gb2312` 成功运行 5 帧并从新路径读取字模（40 次模拟 flash 读取）；`git diff --check` 通过。

## 第 45 轮：中文拼音输入法 project_Demo

- 新增独立应用 `project_Demo/chinese_ime/`，使用 TextInput 接收拼音组合串，应用层小词库生成候选词，Button 支持候选点击上屏、回车选中首项和候选分页。
- 输入法 UI 复用 YMGUI 的焦点、UTF-8 编辑和 GB2312 外部字模回退；词库、提交缓冲和分页状态保留在 app 侧，不扩张控件公共 API。
- 同步 `README.md`、`project_Demo/README.md` 和 `docs/当前状态.md` 的完整应用数量与清单。
- 验证：独立 CMake 构建通过；`SDL_VIDEODRIVER=dummy ./build/rgb565/project_Demo/chinese_ime/chinese_ime 30` 自检通过（候选匹配、点击上屏、分页），进程退出码为 0。

## 第 46 轮：补齐 GB2312 单字拼音候选

- 明确 `tools/gb2312_glyphs.bin` 只存字模 bitmap，不包含拼音或词频，不能直接充当输入法词库。
- 从系统 IBus table 的拼音数据生成 `project_Demo/chinese_ime/pinyin_gb2312.inc`，覆盖 YMGUI GB2312 字集的 6756 个单字、共 8864 条“拼音→单字”映射，并保留多音字全部读音；应用自有短语词库仍优先，未命中时回退单字候选。
- 英文继续由 YMGUI 虚拟键盘直接输入，中文使用同一 QWERTY 键盘组成拼音；没有引入 Qt、Maliit、Fcitx 或 Rime 运行时。
- 验证：输入 `a` 的完整单字回退、既有短语/分页/中英文按键自检均通过；独立构建、`tools/check_repo.sh` 和 `git diff --check` 全部通过。

## 第 47 轮：拼音前缀候选

- 修正输入 `z` 等未完成拼音时候选栏无反馈的问题。精确短语仍优先，未命中时按当前拼音前缀扫描完整 GB2312 单字表，最多显示当前页容量的候选。
- 增加 `z` 前缀自检，确认首字母输入即可得到候选；空组合串保持候选栏清空。

## 第 48 轮：用户习惯频率排序

- 候选排序不再只依赖静态“常用字”表；每次候选上屏都会累加运行时命中次数，后续相同拼音候选按命中次数降序、同频保持原始顺序。
- 统计表固定 64 项、仅驻留 RAM，适配裸机约束；未引入文件系统持久化。自检验证连续选择“尼”后 `ni` 首候选自动调整为“尼”。

## 第 49 轮：全前缀候选参与排序

- 修正候选池过早截断的问题。输入 `z` 或继续输入 `h` 时，先收集全部匹配 GB2312 单字，再按用户命中次数排序，最后按页显示 5 项；不再只对前 16 项排序。
- 候选池容量为 8192 个指针，覆盖当前生成表的全部 6756 条映射和应用短语；候选内容仍由 YMGUI Button 渲染。

## 第 50 轮：候选分页懒排序

- 前缀查询改为按页增量选择：保存当前拼音和已选候选，第一页只扫描并选出前 5 项；点击下一页时再从完整匹配集合中选出下一批，已选项跳过。
- 这样 `z`、`zh` 等前缀仍可覆盖全部匹配字集和用户频率排序，但不会在输入首字母时一次性构造完整候选排序结果。

## 第 51 轮：多音字读音补全

- 修正生成器只取每个字第一个拼音的问题；现在保留 IBus 表提供的全部读音，例如“曾”同时支持 `ceng` 和 `zeng`。
- 映射总数从 6756 条增加到 8864 条，增加 `zeng` 回归测试并确认“曾”可在懒分页中找到。

## 第 52 轮：常用短语与首字母缩写

- 明确单字拼音表不会自动产生词语候选；新增应用层短语表入口，补充 `wm -> 我们` 等首字母缩写，避免只输入每个音节首字母时完全无提示。
- `wm` 回归测试验证首候选为“我们”；更大规模的短语/用户词库仍应作为独立数据文件生成，不混入字形 blob。

## 第 53 轮：短语与单字合并排序

- 短语候选增加完整拼音和首字母索引（如 `women` / `wm`），并与同一查询下的单字候选进入同一个懒排序池。
- 因此输入 `wo`、`wm`、`ni` 等组合时，词语和单字会共同按用户命中次数排序，不再分成两套候选列表。

## 第 54 轮：词语子序列缩写

- 短语匹配增加保持顺序的子序列规则；`wom`、`wome`、`wme` 均可命中“我们”，同时仍支持 `women` 和 `wm`。
- 该规则只用于词语索引，单字仍使用完整拼音前缀，避免把任意字母跳跃误当成单字拼音。

## 第 55 轮：色深构建路径文档修正

- 补充默认 RGB565 与 RGB888 矩阵构建的实际目录约定：`build/rgb565/project_Demo/<项目名>/` 和 `build/rgb888/project_Demo/<项目名>/` 分开配置、编译和运行。
- 在 `project_Demo/README.md`、`docs/当前状态.md` 和跨平台移植文档加入 RGB888 命令示例，避免修改后误运行默认目录中的旧二进制。

## 第 56 轮：RGB565 路径统一

- 进一步核对构建脚本后确认 RGB565 也必须与 RGB888 隔离；`build_all.sh` 的 project_Demo 构建目录统一改为 `build/rgb565/project_Demo/<项目名>/`。
- 文档中的默认 project_Demo 命令同步改用 RGB565 前缀，并保留 RGB888 对应示例，避免把无色深前缀目录误当作当前产物。

## 第 57 轮：标准 QWERTY 虚拟键盘布局

- `chinese_ime` 重排为标准桌面键盘结构：中文标点行、数字行、QWERTY 三行字母和底部中英/Shift/退格/空格/回车功能行。
- 数字和标点在中文模式下会先提交当前候选再上屏，英文模式直接追加，全部通过 YMGUI Button 和事件回调实现。
- 验证：RGB565 独立目录 `build/rgb565/project_Demo/chinese_ime/` configure、build 和 30 帧无头自检通过。

## 第 58 轮：系统词语数据与候选区重排

- 使用系统 IBus `phrase.txt.bz2` 词语/频率和拼音表生成 `phrases_ibus.inc`，当前收录 30000 条词语；源码不再维护短语数组。
- 候选 UI 改为左右翻页按钮夹一块只读文本，按 `1 我  2 沃  3 卧` 格式展示，避免候选按钮和翻页控件重叠。

## 第 59 轮：明确拼音词典与许可兼容

- 推翻“从单字默认读音拼出词语拼音”的临时生成方式。曾验证 libpinyin model20 能提供明确音节和权重，但其模型为 GPL-3+，不适合直接嵌入 Apache-2.0 仓库；最终改用同为 Apache-2.0 的 `rime-pinyin-simp`。
- 新增 `tools/gen_ime_dict.py`，从 Rime 词典生成 7291 条单字读音和 30000 条明确拼音词语。运行时删除手写短语、常用字和特殊词条数组，单字/词语统一按用户命中次数优先、词典初始频率兜底进行懒分页选择。
- 候选区改为 YMGUI 自绘只读视图，编号青色、词语白色；中文组合态数字键 1..5 选择候选。RGB565 构建、自检、仓库检查和截图布局检查通过。

## 第 60 轮：音节感知短语匹配与完整 Rime 词表

- 替换第 54 轮的任意字符子序列规则。短语查询现在只允许把输入串切成连续音节的非空前缀：`wo'men` 可由 `wm`、`wom`、`wme`、`wome` 或 `women` 命中，`wn`、`wem` 等跨音节跳字母输入不会误命中。
- 取消生成器默认截取 30000 条词语的上限。Apache-2.0 `rime-pinyin-simp` 经 YMGUI GB2312 码点过滤后保留全部 47278 条合法词语；运行时仍按需扫描并只缓存用户请求的候选页，不因词表扩大而一次性排序全部结果。
- 候选只读视图按五个稳定槽位分别裁剪，长词不会覆盖相邻编号或右侧翻页按钮；候选缓存增加容量边界保护，状态栏显示页码而不把当前缓存的 5 项误报成候选总数。用户词频项扩到可完整保存 8 个汉字的 UTF-8 词语，长词选择也能正确参与学习排序。
- `tools/check_repo.sh` 增加输入法数据检查：固定验证 7291 条单字读音、47278 条词语以及“我们/音乐/重庆/曾(ceng/zeng)”关键记录。自测新增音节组合正例、非法子序列反例，并逐项确认 `wom/wome/wme` 的首候选确为“我们”。
- 验证：RGB565 与 RGB888 独立构建通过；RGB565 30 帧无头自测通过；仓库检查和 `git diff --check` 通过。

## 第 61 轮：词条分段组合候选

- 排查 `kknd` 不出现“看看你的”：Rime 源词典分别包含 `看看 = kan'kan/kk` 与 `你的 = ni'de/nd`，但没有“看看你的”整句；此前引擎只查询单条词典记录，不具备桌面输入法常见的分词组合层。
- 增加受限束搜索：沿输入串按音节前缀匹配已有词条，用组成项平均词频和分段数惩罚为组合候选赋初始分数，再与直接词语、单字以及用户选择次数进入同一套排序。该机制从生成词库动态组合，不维护句子特例数组。
- 组合单元同时接受首字母、完整拼音和每音节前缀混输；`kknd` 与 `kankannide` 的首候选均验证为“看看你的”。进一步把单字表接入组合搜索并取消四字母启动门槛，`kkn` 可由 `看看 + 你` 得到首候选“看看你”。组合束宽固定为 6、最多保留 12 个预测项，控制 RAM 和每次按键的搜索量；直接词典候选仍按原有懒分页覆盖全部匹配记录。
- `tools/check_repo.sh` 增加“看看”和“你的”源词条校验；用户词频字符串容量扩大到 96 字节，可学习组合后的较长候选。

## 第 62 轮：可扩展用户词库

- 新增 `project_Demo/chinese_ime/user_phrases.tsv`，每行以制表符填写“词语、分音节拼音、初始词频”。`tools/gen_ime_dict.py` 默认自动合并该文件，也支持重复传入 `--extra-dict` 合并多个外部 TSV；词语数据仍由生成器进入 `.inc`，不在 C 源码维护手写数组。
- 用户词库支持 1 个单字或最长 31 字的词语，接受空格或单引号分隔音节；生成时严格检查音节只能含 ASCII 字母、汉字必须存在于 YMGUI GB2312 字形索引、词长必须等于音节数、词频必须在 uint32 范围。错误会带文件名和行号终止，不会静默漏掉自定义词。
- 空的默认用户词库重新生成后仍为 7291 条单字读音、47278 条词语；RGB565 构建和包含 `kkn/kknd/kankannide` 的无头自测通过。

## 第 63 轮：按文字宽度动态分页与单字母单字优先

- 删除候选区固定五等分槽位和 `页号 * 5` 下标。当前页从排序缓存的起始位置出发，用默认字体的真实像素宽度依次装入候选，最多支持数字键 `1..9`；下一项放不下时不绘制、不截字，完整成为下一页首项。上一页通过已缓存候选重新推导页边界，因此动态页长不会造成重复或漏项。
- 懒加载继续有效：布局只要求生成当前页以及用于判断溢出的下一项，不提前排序全部候选。状态保存当前页起始位置、实际项数和页码；回车、空格、数字选择与前后翻页全部改用动态页索引。
- 单字优先条件不按输入长度判断，而按单字词典是否仍有匹配项判断。`k`、`wo`、`zhong` 会先在 7291 条单字读音中按用户命中次数和字频排完全部匹配单字，再开始加入词语；`wm`、`nihao`、`kkn` 等没有可能单字读音的查询直接让词语和组合预测竞争。
- 自测覆盖动态首页容量、前后翻页边界、`k` 首页全为单字、“沃”位于 `wo` 首个词语之前，以及原有候选/组合/学习行为。`kkwdm` 截图验证本页只装入 4 个完整候选，末项“看看我对面”没有被下一页按钮覆盖。

## 第 64 轮：候选点击、实体数字选择与多页符号栏

- 候选只读视图增加点击事件，并复用动态排版的真实文字宽度生成每项命中区；点击编号、文字或其后的间距都会选择对应候选。候选仍由一个 YMGUI 自绘对象承载，不退回多个固定按钮。
- 给聚焦的 TextInput 包装应用层键盘事件：中文组合态下实体键盘 `1..9` 选择本页对应项并阻止数字写入拼音，其余事件继续交给原 TextInput；屏幕数字行同步支持本页最多 9 项，超出实际项数时仍输入普通数字。
- 标点行调整为一个符号页切换按钮加 9 个符号按钮，内置“中标、英标、括号、符号”四页。符号页可独立循环；中英文模式按钮切换时自动选择中文或英文常用标点页，中文组合态点标点仍先提交首候选再追加符号。
- 自测新增候选坐标点击、实体数字选择、符号页循环和符号上屏；RGB565/RGB888 构建、无头自测和截图检查通过。

## 第 65 轮：词语可裁剪与外部 BIN

- `chinese_ime` 新增 `YMGUI_IME_PHRASES=EXTERNAL/OFF/EMBEDDED` 三种 CMake 模式。默认 `EXTERNAL` 不编译 47278 条 C 词语数组，启动时仅在 `YMGUI_IME_PHRASE_BIN` 指定文件存在且合法时加载；`OFF` 不访问词语文件且只保留单字候选；`EMBEDDED` 保留原有编入程序方案。
- `tools/gen_ime_dict.py` 在生成 `phrases_rime.inc` 的同时生成 `phrases_rime.bin`。BIN 使用版本化魔数、记录数、字符串区长度、定长偏移记录和去重 NUL 字符串区；当前 47278 条词语文件约 1.6 MiB。外部加载器验证文件总长、数量上限、乘法边界、每个字符串偏移和 NUL 终止，缺失或损坏均记录日志后安全退化为纯单字。
- 新增 `tools/verify_ime_dict.py`，仓库快速检查固定验证外挂词库的魔数、47278 条记录、文件长度和全部字符串偏移。用户 TSV 重新生成时会同步更新内嵌和外挂两种产物。
- 验证四条路径：默认外挂成功加载并通过完整自测；不存在的外挂路径自动退化并通过纯单字/数字选择自测；`OFF` 构建通过纯单字自测；`EMBEDDED` 构建通过完整词语和组合自测。

## 第 66 轮：可扩展字母桶索引与连续扫描游标

- 外挂格式升级为 `YMIMEP2`。头部显式记录 `header_size`、`record_size` 和 `bucket_count`，随后保存 27 个边界偏移描述 26 个首拼音字母逻辑桶；词语仍位于单个 BIN，避免 26 文件的部署、打开和一致性成本。生成器同时为 `EMBEDDED` 产物生成相同桶偏移。
- 词语按“首字母、静态词频降序、拼音/词语稳定次序”生成。组合束搜索在每个输入位置只扫描 `query[pos]` 对应桶；普通候选保存当前桶的扫描游标和一个待选项，懒分页继续时从原位置推进，不再为每个候选重扫 47278 条词语。
- 运行时用户学习保持优先级：查询初始化时只在当前桶收集命中 64 项用户统计的小集合，按用户次数/静态词频与组合候选竞争；未学习词直接沿桶内静态顺序读取，无需排序整个桶。新增回归确认学习后的 `wm` 非首项能提升到桶首。
- P2 加载器和 `verify_ime_dict.py` 校验头部尺寸、记录尺寸、桶数量、边界单调性、每条拼音所属桶及原有字符串安全条件。当前最大 `z` 桶为 4636 条；同机完整无头自测由索引前约 1.07 秒降到约 0.55 秒，峰值 RSS 由约 14.9 MiB 降到约 12.4 MiB（仅作同序列相对参考）。

## 第 67 轮：单字查询缓存与组合扫描去重

- 生成器为 7291 条单字读音增加与词语一致的 26 个首字母桶边界。查询初始化只扫描对应单字桶一次，过滤当前拼音前缀、按汉字去重并按“运行时用户次数、静态字频、稳定拼音/汉字次序”排序到指针表；懒分页之后用游标顺序读取，不再为每个候选重新遍历全部单字并做已选检查。
- 组合束搜索调整循环顺序。原实现对同一输入位置的每个束状态分别扫描词语桶和全部单字表，最多将扫描放大到束宽 6 倍；现在每个位置只扫描一次对应词语桶和单字桶，计算匹配长度后再扩展到该位置已有的全部状态，候选语义和评分不变。
- 查询期单字缓存只保存最多 7291 个常量条目指针，不复制拼音或汉字；额外静态 RAM 在 64 位约 57 KiB、32 位约 29 KiB。该取舍适合当前 Demo，极小 RAM 移植后续可增加编译开关选择慢速游标策略。
- 原有完整自测继续覆盖多音字、单字优先、动态分页、词语/组合、单字与词语用户学习、鼠标/数字选择和符号页。30 帧无头运行受固定 480ms 延时主导，稳定约 0.53 秒；1 帧完整自测序列稳定约 0.06 秒，仅作为同机相对参考。

## 第 68 轮：中文单字与英文词库独立外挂

- 将原来无条件编译的 7291 条中文单字读音生成 `pinyin_gb2312.bin`，新增 `YMGUI_IME_CHARS=EXTERNAL/EMBEDDED/OFF`。默认外挂模式按独立魔数、记录尺寸、26 个字母桶、字符串边界和文件长度校验；缺失或损坏只关闭单字候选，已安装的中文词语仍能按自身拼音工作。`EMBEDDED` 保留无文件系统固件的兼容路径。
- 新增 `english_words.bin` 和 `YMGUI_IME_ENGLISH=EXTERNAL/OFF`。数据来自 Apache-2.0 `wordfreq 3.1.1` 的 large 英文频率表，过滤为 288996 个 2～31 字节 ASCII 小写单词；生成器把频率顺序写入 26 个首字母桶。英文模式有词库时进入前缀组合和候选分页，按运行时用户次数、初始常用度排序；词库缺失或关闭时保留原有逐字直接上屏。
- 中文单字、中文词语、英文单词三份数据互相独立。CMake 分别提供路径变量 `YMGUI_IME_CHAR_BIN`、`YMGUI_IME_PHRASE_BIN`、`YMGUI_IME_ENGLISH_BIN`；任一外挂异常不会连带关闭键盘、标点或其他词库。查询指针表按实际加载条数动态分配，不再依赖编译期单字数量。
- `tools/gen_ime_dict.py` 同步生成 C1 单字 BIN、P2 词语 BIN 和可选 E1 英文 BIN；`tools/verify_ime_dict.py` 扩展为校验三种格式，并固定检查“曾”的多音字记录和 `th -> the` 常用度顺序。英文数据来源、过滤和许可单列于 `project_Demo/chinese_ime/ENGLISH_DICTIONARY.md`。

## 第 69 轮：实体键盘式虚拟键位

- 输入法键盘从“所有字母同层排列”调整为实体 QWERTY 的功能位置：第一字母行两侧放 `Tab`/`Bksp`，第二行右侧放 `Enter`，第三行左侧放 `Shift`，底部放 `Ctrl`、`Alt`、中英切换、加宽空格以及右侧 `Alt`/`Ctrl`。数字和标点仍保持独立行，候选区坐标不变。
- `Ctrl`、`Alt`、`Shift` 改为点动锁存。激活后按钮显示 `*`，再次点击解除；虚拟 `Ctrl+A/C/X/V/Z/F` 映射到 YMGUI 已有编辑快捷键，`Tab` 直接发送 `GY_KEY_TAB` 并恢复输入框焦点。模式切换会清除修饰键锁存，避免跨中英文状态残留。
- RGB565/RGB888 project_Demo 独立目录重新构建，1 帧自测通过；原有候选、分页、中文组合、英文词库和外挂降级测试继续通过。

## 第 70 轮：实体键盘主层布局

- `chinese_ime` 主键区进一步按实体键盘骨架排布：保留数字/标点行，字母第一行两侧放 `Tab` 与 `Bksp`，第二行右侧放 `Enter`，第三行左侧放 `Shift`，底部保留 `Ctrl/Alt/中英/空格/Alt/Ctrl`。
- 字母三行使用实体键盘常见横向错位，功能键位置稳定；符号页暂保留为下一步独立键盘层，避免主层和符号层共用同一批按钮时出现状态与命中区域耦合。
- RGB565/RGB888 两套 project_Demo 重新构建通过，30 帧无头输入法自测继续通过。
## 第 71 轮：双字符键与锁存视觉

- 数字行改为实体键盘式双字符键：`1!` 到 `0)`，主字符和 Shift 字符在同一个稳定矩形内绘制；普通状态主字符为白色、Shift 字符为灰色，Shift 锁存后颜色交换。
- `Ctrl`、`Alt`、`Shift` 锁存后不再追加 `*`，改为将按钮文字绘为黑色；未锁存时文字为白色。左右 Ctrl/Alt 镜像按钮共享同一锁存状态。
- 键盘增加独立的 `Esc/F1..F12` 功能键行，并重新压缩行高与间距；`Tab`、`Bksp`、`Enter`、`Shift` 均有独立矩形。RGB565/RGB888 30 帧自测通过。
## 第 72 轮：主键盘功能键收敛

- 按最终确认的虚拟键盘方案移除右侧重复 Ctrl/Alt；最后两行固定为 `Shift/中英/.../键盘/设置` 和 `Ctrl/Alt/符号/空格/方向键/PrintScreen`。
- 补上独立 `Caps` 键，字母区按普通键宽排布；`Enter` 使用剩余宽度，`;:` 与 `\"'` 保持普通字母键宽度。
- 数字双字符键继续使用主字符/Shift 字符双颜色绘制，所有功能键重新划分稳定矩形并通过 RGB565/RGB888 编译验证。

## 第 73 轮：截图驱动的键位校正与完整符号层

- 实际截取 640×480 RGB565 画面后发现功能键/数字行顺序、数字行外围符号和字母行右侧符号与确认稿不一致，废弃零散坐标并按 `x=24..616` 重算六行固定矩形：`Esc/F1..F12`、完整数字行、Q 行、A 行、Z 行和底部功能行。
- 主键盘补齐 `` `~``、`-_`、`=+`、`[{`、`]}`、`\|`、`;:`、`'\"`、`<,`、`>.`、`/?`。所有双字符键共用主/Shift 双色绘制和实际输入逻辑；Caps 与 Shift 按异或规则决定字母大小写。
- `符号` 改为单向进入独立覆盖层，主键盘不再显示“中标/英标”循环。符号层提供 36 个符号位和 `常用/中英/返回/数学/角标/序号/希腊/拼音/箭头` 九个底部键，只有 `返回` 回主层。
- 分别截取主键盘和符号层检查边界、间距、文字裁剪与候选区保留情况；RGB565/RGB888 30 帧自测、仓库检查和 `git diff --check` 通过。

## 第 74 轮：符号页字形覆盖自查

- 重新截取主键盘、Shift 锁存态以及全部七个符号分类页。六行主键盘和 4×9 符号层没有覆盖或裁切，但数学、角标、序号和箭头页暴露出部分按钮有文本却无字形的问题。
- `gen_font.py --gb2312` 的补充集加入输入法所需的数学、上下标、圈号、拼音和箭头码点；补充符号改用覆盖更完整的 DejaVu Sans 光栅化，避免 Noto Sans CJK 对部分码点输出 `.notdef` 方框。外部字模由 7448 字形/953344 字节扩展到 7528 字形/963584 字节，索引、blob、校验脚本和公开文档同步更新。
- 输入法无头自测新增七个分类全部 270 个符号文本的码点覆盖检查，任一非 ASCII 符号没有进入 `YMGUI_GB2312_cps` 就直接失败。Demo README 同步移除旧的“中标/英标循环”说明，改为独立符号覆盖层。
- 符号分类不再受 36 项数组硬上限约束：覆盖层向上利用原标点栏空间，顶部新增独立上一页/页码/下一页，中间保持每页 4×9 个符号位，底部九个分类键不变。分类数据预留 72 项，切分类归第一页，末页自动隐藏空位；常用分类加入第二页并把往返翻页和第二页点击纳入自测。

## 第 75 轮：复用候选翻页与中英文符号表

- 删除符号覆盖层内部重复创建的上一页、页码和下一页控件；候选栏原有左右按钮在符号模式下改为翻符号页，中间只读区域显示“中文/英文 + 分类 + 当前页/总页数”，返回主键盘后恢复候选绘制和候选翻页。
- 中文常用和英文常用拆为两份独立符号表。主键盘的数字/标点双字符键始终按实体键盘输出 ASCII；符号层点击 `中英` 时立即切换到对应常用符号并回到第一页，不再让英文模式继续显示中文逗号、句号和引号。
- 单分类容量由 72 扩至 108，补充货币/版权、集合与逻辑、上下标与单位、带括号序号、完整希腊字母、大小写拼音音调和更多方向箭头。七分类当前共 489 个条目，数学和箭头均可进入第三页。
- 外部字模同步扩展到 7672 字形、982016 字节；自测覆盖中英文符号换表、复用候选按钮往返翻页、第二页点击和所有符号码点，截图检查中文/英文常用页、数学第二页和箭头末页均无覆盖或缺字。

## 第 76 轮：键盘区组合镜像与零候选回车

- 利用候选栏与 Esc 行之间的 `y=238..268` 空白新增只读组合镜像框。视图不维护第二份缓存，绘制时直接读取原 TextInput；`updateCandidates()` 统一触发失效，中文显示拼音缓存，英文显示英文缓存。
- 符号覆盖层与镜像框使用同一区域但创建顺序更高，进入符号页后完整覆盖镜像，返回主键盘后自动恢复，不增加显隐切换状态。
- 中文回车提交改为两路：当前有候选就选择首项，没有候选但组合串非空则原样追加到上屏区并清空 TextInput。实体 Enter 的 submitted 回调与屏幕 Enter 共用同一实现。
- 自测新增无候选 `v` 序列的虚拟/实体 Enter 原样提交；RGB565/RGB888 30 帧自测通过，主键盘与符号覆盖层分别截图确认没有重叠。

## 第 77 轮：可编辑上屏区与组合/候选分行

- 输入法不再用 Label 和应用层字符数组维护上屏文本，改为 4096 字节、开启折行的多行 EditView。用户可用鼠标定位光标和选区，候选、英文、符号、空格与换行都在当前光标插入，有选区时直接替换。
- 新增公开接口 `YMGUI_EditView_InsertText()`，复用 EditView 内部的选区删除、容量控制、行表重建、光标滚入可见区、变更回调和单级撤销。`test_editview` 覆盖光标中插入、选区替换和撤销。
- 候选翻页行中间改放唯一的真实组合 TextInput，原先的只读组合镜像删除；候选文本移到下一行并扩为全宽。符号层从 Esc 行开始覆盖，仍保留上方的组合框、复用翻页按钮和全宽页码文本。
- 修正虚拟退格在组合串为空时删除 EditView 内容的焦点状态：程序聚焦 EditView 后先进入编辑态，再注入 Backspace，保留用户最后的输出光标位置。RGB565/RGB888 编译和 30 帧自测通过；主键盘与符号层 640×480 截图均确认无覆盖或裁切。

## 第 78 轮：实体直出、虚拟快捷键与键盘收起

- 主键盘双字符键改为按中英文模式动态取表。中文模式显示并输出 `！/￥/…/（）/【】/；：/《》/，。/？` 等中文标点，英文模式恢复 ASCII。模式切换不再清除 Shift 锁存；回归覆盖“中文 Shift+9 输出 `（`，保持 Shift 切英文后同键输出 `(`”。
- 组合 TextInput 的键事件增加字面分流：英文字母仍进入拼音/英文缓存；实体空格、数字和 ASCII 标点先提交当前组合，再按当前语言映射立即上屏。有候选时 `1..9` 仍优先选择本页候选；回归覆盖无候选缓存后的中文逗号和数字直出。
- 虚拟 `Ctrl+A/C/X/V/Z/F` 不再向当时获焦的组合 TextInput 注入命令，改为直接调用上屏 EditView 的全选、剪贴板和撤销 API；新增虚拟 Ctrl+A 选区范围回归。
- 组合缓存为空且不在符号层时，原翻页键动态显示为 `收起/展开`。收起状态隐藏 Esc、数字、字母和底部功能行，仅保留组合与候选两行供实体键盘使用；输入组合后自动恢复翻页文字。RGB565/RGB888 30 帧完整自测通过，展开/收起截图确认无残影、覆盖或裁切。

## 第 79 轮：中文空组合退格

- 修复虚拟 `Bksp` 只在英文模式将空组合的退格转发给 EditView，导致中文空组合时按键无效。同时修复程序恢复 TextInput 焦点后处于选择态，实体 Backspace 被 TextInput 忽略的问题；输入法事件包装层现在优先截获实体 `GY_KEY_BACKSPACE`，与屏幕 `Bksp` 共用删除逻辑。
- 当前规则统一为：组合 TextInput 非空时删除组合字符；组合为空时进入上屏 EditView 编辑态，删除当前光标前一个 UTF-8 码点。回归覆盖“中文空组合下 `你好 -> 你`”和“选择态组合 `ni -> n`”两条路径；RGB565/RGB888 30 帧完整自测通过。

## 第 80 轮：实体键状态统一与 EditView 内边距

- HAL 新增可选 `YMGUI_Inject_SetKeyFilter()`：应用层可在焦点对象前观察并消费按键的按下/抬起；未消费按下保持原有 `GY_EVENT_Key` 行为，抬起只用于状态维护。新增 `GY_KEY_SHIFT/CTRL/ALT/CAPS` 原始键码，事件测试覆盖消费、透传和抬起可见性。
- SDL 桥接开始处理 `SDL_KEYUP`。Shift/Ctrl/Alt 按实际按住状态驱动输入法，Caps 忽略系统重复后点动翻转；Tab/Backspace/Enter 也补发释放。窗口失焦、后台和退出会释放残留修饰键。普通可打印文本仍只走 `SDL_TEXTINPUT`，避免重复注入。
- `chinese_ime` 将实体字母、数字、标点、空格、Backspace、Enter、Tab 和 Ctrl 命令映射回已有虚拟按键回调。虚拟 Shift/Ctrl/Alt 保持点动锁存，实体键只在按下期间叠加生效；Caps 两种来源共用锁存，按键外观也复用同一状态。修正共用标点/数字回调在无候选组合串下未先原样提交的问题。
- EditView 新增公开 `YMGUI_EditView_SetPadding(left, top, right, bottom)`。默认仍为左右 4px、上下 0，兼容旧界面；内边距统一参与折行宽度、纵向滚动视口、点击定位、文字/选区/光标绘制与裁剪。输入法上屏 EditView 设置四边 4px，首行不再紧贴顶部边框。
- `test_editview` 新增内边距对折行、滚动上限和点击定位的回归。根工程 `test_event/test_editview` 通过；RGB565/RGB888 输入法均完成构建和 30 帧无头自测。
- 项目用户词库新增 `嘎达 = ga'da`（初始频率 50000）和 `尼玛 = ni'ma`（初始频率 100000）；后者高于“你 + 吗”的组合评分，保证进入 `nima` 首页。重新生成后词语总数为 47280；输入法自测和 BIN 校验新增两条回归，防止后续词典重生成丢失扩展词。
- 修复组合 TextInput 的屏幕字母和退格绕过控件编辑逻辑、始终操作字符串末尾的问题。现在实体与虚拟字母统一走 TextInput 当前光标插入/选区替换，`Bksp` 也按组合光标删除；内部转发使用旁路直接进入 TextInput 原始事件回调，避免 Backspace 被包装层反复截获并无限递归闪退。自测覆盖 `gda` 中间插入 `a` 得到 `gada`，再在同一位置退格恢复 `gda`。


## 第 81 轮：video_stidio 接入本仓库并跑通

- 背景：导入的剪辑器仍使用旧仓库名称和 `extern_lib/YMGUI` 引用，FFmpeg 路径缺失，无法在 YMGUI 根目录构建。改为复用 `project_Demo/ymgui_app.cmake`，目标、窗口标题和界面统一为 `video_stidio`；删除应用内 Shotcut 对照文档、旧项目引用及失效操作说明，重新生成运行截图。项目文件头统一为 `VIDEO_STIDIO 1`，不再读取旧名称文件头。
- 接入用户提供的 `extern_lib/FFmpeg` 7.1.5 源码，用 CMake ExternalProject 在应用 build 目录独立配置和增量编译静态库；不复制旧仓库 YMGUI，也不向第三方源码目录生成构建文件。缺少源码时支持 pkg-config 系统开发库，`STUDIO_FFMPEG_SOURCE` 指定源码位置或留空强制系统模式，`STUDIO_FFMPEG_JOBS` 控制并行数。媒体任务依赖 POSIX/C11，仅支持 Linux/RGB565，配置阶段明确拒绝其他平台或色深。
- 保留现有视频编辑功能：后台导入、8 视频轨、跨轨拖放/修剪/分割/波纹删除、撤销重做、原子项目读写、缓存与代理预览、入出点缩略图和项目快照导出。FFmpeg CLI 继续用于代理和 H.264 编码；原生库用于预览和导出源帧读取。解码器改为按选中视频流的 codec ID 查找解码器，兼容系统 FFmpeg 4.4 与源码 7.1 的 API 类型差异。
- 集成测试不再读取其他应用的个人样片：CTest fixture 自动生成 96 秒视频，媒体/导出测试统一使用配置时选定的 FFmpeg CLI 路径。独立工程共 5 个 CTest（1 个素材 fixture + 4 组功能测试）；根工程仍为 35 个，完整应用数由 11 增至 12。README、应用说明、当前状态和仓库计数检查同步更新。
- 验证：FFmpeg 7.1.5 静态模式完整构建成功，独立 CTest 5/5 通过（约 10 秒），覆盖解码/帧替换、鼠标拖放/修剪/取消、撤销重做、绑定、代理、缩略图及真实 H.264 输出的帧数/尺寸/裁剪/遮挡/黑帧/取消/文件保护。30 帧无头空项目启动成功；集成测试截图确认中文、播放器、双轨片段和缩略图正常，连续拖头 30 次获得 30 次画面更新。真实桌面窗口导入样片后运行 120 帧并正常退出；最初受沙箱显示连接限制，使用桌面权限运行后成功。仓库自检与 `git diff --check` 通过。当前导出仍仅为 720p/25 fps 无音频，不包含音频混音、透明叠加、滤镜或转场。

## 第 82 轮：第三方依赖目录说明与真实素材截图

- 按使用范围明确 `extern_lib/` 是供 `project_Demo/` 应用按需引用的第三方依赖目录，当前 FFmpeg 由视频剪辑器使用；源码与许可证保留在该目录，编译产物放在对应应用 build 目录。根 README、project_Demo 入口、当前状态及新增 `extern_lib/README.md` 同步说明。
- README 视频剪辑器预览改用仓库已有的 `project_Demo/video_player/卖瓜.mp4`。通过应用真实导入和“追加到轨道”动作，将完整素材加入 V1，适配整个项目并定位到 12 秒；等待预览代理和首尾缩略图就绪后，从 YMGUI 实际 framebuffer 保存 1600×1000 截图。根 README 与应用 README 共用该图片，应用说明补充复现步骤。
- 验证：截图辅助程序断言导入成功、V1 存在片段、目标帧显示及首尾缩略图就绪；视觉检查素材名、视频画面、片段属性、时间线和中文文字正常。文档链接、仓库自检及 `git diff --check` 通过。本轮仅更新文档和图片，应用功能与 CTest 数量保持不变。

## 第 83 轮：面向 AI 编程助手的库使用指南

- 背景：已有文档分别记录完整能力、API 和内部架构，AI 接手应用任务仍需自行串联目录、控件、生命周期与构建入口。新增 `docs/AI_GUIDE.md`，按任务提供目录分工、27 控件选型表、最小桌面应用、对象 / 图片 / 字符串 / subject 生命周期、事件与刷新技巧、中文字库、色深配置、验证路径、常见问题和示例索引。
- 指南强调代码和测试优先，不另建功能事实来源；保留核心库、平台适配、应用及 `extern_lib/` 的职责。明确控件私有 `user_data`、双向绑定占用 changed 回调、TextInput 字符串借用、移动时旧新区域失效、band 自绘坐标、输入 / 字体全局状态和独立应用重建等容易误用的细节。
- `AGENTS.md` 增加 AI 使用指南的阅读步骤，根 README、docs 导航和当前状态补充入口；架构文档修正库层数、双缓冲已启用、FlushReady 函数名及实际配置宏，避免后续助手继承过期结论。
- 验证：从 Markdown 提取原样 C 示例，在 build 临时目录只调整 CMake 公共脚本引用位置，分别按 RGB565 / RGB888 完整编译并无头运行 30 帧，均返回 0；不增加正式应用或 CTest 数量。文档链接、仓库自检和 `git diff --check` 通过。本轮不修改库实现。

## 第 84 轮：参考 YMGRE 的独立 lib 包与外部接入

- 背景：用户希望参考 `0502_YMGRE/build/YMGRE_libs` 提供可直接接入的 YMGUI 库包。检查其打包脚本、CMake 目标、示例 host 与验收方式后，采用独立 `sdk/` 构建入口；`SDL_LCD/` 继续只做平台适配，`extern_lib/` 继续供 project_Demo 的第三方依赖使用。YMGRE 工程仅只读检查，不复制它的库或许可。
- 新增 library-only CMake 与 Python 打包器，一次生成 RGB565 / RGB888 核心静态库和对应的可选 SDL 静态库，Release/PIC；导出可重定位的 `YMGUI::ymgui`、`YMGUI::sdl`，包内包含匹配头文件、纯核心和 SDL 示例及可执行文件、GB2312 字模、手册、原有 LICENSE、构建元数据和校验清单。生成目录为 `build/YMGUI_libs`，同时输出 tar.gz 和 SHA-256。不把 Demo、业务应用或 FFmpeg 收入库包。
- 核心库与 SDL 依赖分开；`--without-sdl` 可生成纯核心包，消费端 `YMGUI_WITH_SDL=OFF` 可禁用适配。默认依赖可用时自动提供 SDL 目标，兼容 YMGRE 无组件的 `find_package(YMGUI)` 调用。SDK 仅支持原生 Linux，检查系统、架构、指针位宽与色深；包内头文件拒绝坐标宽度、色深和脏区容量的常见 ABI 冲突。需要裁减或跨平台时仍从源码构建。
- YMGRE host 还使用窗口 ID 和标题接口，因此补充 `SDL_LCD_WindowId` / `SDL_LCD_SetTitle`，并扩展原 `test_sdl_context` 验证初始化前、有效窗口和销毁后的行为。根工程仍为 35 个 CTest；API、移植文档、README、AI 指南和当前状态同步更新。
- 验证：RGB565 / RGB888 根工程各 35/35 通过；SDK 复制到仓库外后，两种色深各完成纯核心 1 项和 SDL 2 项 CTest，覆盖绘图、按钮注入、绑定、空闲刷新与有限帧窗口运行。核心 archive 无 SDL 未解析符号，非法色深和三类 ABI 冲突均被拒绝；`--without-sdl` 整包生成与双色深示例验收同样通过。外部 YMGRE index16 / index32 各 30/30 通过（核心 + 29 个窗口示例，无头有限帧）；构建目录位于临时目录，没有修改外部 YMGRE。只有接入验证成功后才替换带生成器标记的旧包。

## 第 85 轮：将预编译库包纳入仓库分发

- 背景：上一轮提交了 SDK 构建入口，但实际产物位于被 Git 忽略的 `build/`，其他人拉取后不能直接取得预编译库。新增 `releases/`，提交 Linux x86_64 的完整 SDK 压缩包与 SHA-256 文件，分发说明提供校验、解压和示例构建步骤。
- 打包器增加 `--release`：验收通过后将压缩包和校验文件输出到可跟踪的 `releases/`；普通构建继续使用 `build/`，避免每次本地构建都改动入库的分发包。解包目录与构建中间文件仍留在 `build/`。README、SDK 说明 / 手册、AI 指南、文档导航和当前状态同步说明两类目录。
- 验证：重新生成分发包，双色深核心 / SDL 示例均通过，外部 YMGRE index16 / index32 各 30/30 通过。对最终 tar.gz 验证 SHA-256，并解压到仓库外临时目录，包内清单完整，原样运行 `build_demos.sh 16` 后 2/2 CTest 通过。库实现与 SDL 接口未改变。

## 第 86 轮：统一 Demo 与完整应用的构建目录

- 背景：默认构建将控件 Demo 散放在 `build/`，完整应用已使用 `build/rgb565/project_Demo/`，测试脚本另用 `build/verify/`，截图脚本却仍引用旧的 `build/project_Demo/`。这会产生多份缓存，并可能找不到或运行旧的应用。
- 统一默认入口为 `build/rgb565/Demo/` 与 `build/rgb565/project_Demo/<项目名>/`；RGB888 对应 `build/rgb888/`。根工程的核心库、28 个 Demo 和 35 个测试共用 Demo 构建目录，各完整应用保持自己的独立构建目录。根测试脚本复用这套布局，1/8bpp 放在 `build/depth1/Demo/`、`build/depth8/Demo/`，保留 `--build-dir` 供独立实验使用。
- `build_all.sh` 增加 `--depth 16/24`，每次显式配置色深，RGB888 跳过仅支持 RGB565 的 video_stidio；并发默认不超过 8，可用 `YMGUI_BUILD_JOBS` 调整。根构建失败时不再运行旧的测试程序；`-c` 仅清理当前色深的 Demo 和当前源码中各应用的构建目录，不再删除整个 build 和 SDK。
- 截图脚本同步新路径，增加 `--depth`、`--output`，按源码列表枚举所需二进制，缺少文件或程序运行失败都报错；video_stidio 改用 `--frames`，避免把帧数当作待导入文件。新增构建目录专题，同步 README、当前状态、AI 指南、跨平台说明、工具和应用入口。历史缓存保留，避免删除其中的个人文件；新入口不读取旧目录，也不直接移动含绝对路径的 CMake 缓存。
- 验证：`build_all.sh -t` 的 RGB565 根工程和 12 个应用全部构建成功；`--depth 24 -t` 的 RGB888 根工程和 11 个应用成功，根 CTest 各 35/35 通过。核对两套目录均有 28 个 Demo 和对应应用，缓存色深正确；从仓库外调用 `tools/test.sh --depth 24` 通过。非法色深在执行清理或截图前被拒绝，脚本语法检查通过。
- 截图验收：最终脚本在 RGB565 生成 40 张、RGB888 生成 39 张 PNG，全部正常退出，输出位于 build 下的检查目录，未覆盖文档既有图片。仓库自检及 `git diff --check` 通过，build 仍无被 Git 跟踪的文件。


## 第 87 轮：本机构建音乐制作参考软件

- 背景：为后续 YMGUI 音乐制作 demo 体验成熟应用交互，按要求将 LMMS v1.2.2 和 Hydrogen 1.2.6 的源码与子模块下载到 `参考/`，完成本地 Release 构建和安装。
- 增加依赖准备、构建和两款应用的启动脚本，以及面向使用者和 AI 的参考说明。Ubuntu 开发包按 APT 元数据校验后解包到本地前缀，复用已安装运行库；未进行系统包安装。每款应用有独立 build/install/user 目录，启动时打开工程副本并使用 PulseAudio。
- 处理旧版 LMMS 构建宏对中文依赖路径的转义问题：用 `build/music-reference` 符号链接提供英文路径，实际文件仍在 `参考/`。补齐开发库到系统运行库的链接并清除初次探测残留的静态库缓存；Hydrogen 从自身源码目录配置，避免版本检测误用 YMGUI 的提交号。上游源码保持原样。
- 验证：版本输出 LMMS 1.2.2、Hydrogen 1.2.6，实际图形窗口均载入自带示例。LMMS 的 Root84-Initialize 导出约 365.14 秒 WAV，Hydrogen 的 GM kit demo #1 导出约 25.32 秒 WAV，均为 44.1 kHz / 16 位 / 双声道且非静音。脚本语法检查通过；没有运行上游完整测试套件或改变 YMGUI 核心接口。
- 文档检查跳过 `参考/` 下的第三方源码和生成子目录，继续检查我们维护的 `参考/README.md`，避免上游文档的自身链接约定导致仓库自检误报。仓库自检与 `git diff --check` 通过。
- 边界：LMMS 关闭 VST/Carla，其他可选功能取决于本机依赖；Hydrogen 未启用 CPPUNIT。源代码、依赖和生成文件由 `参考/.gitignore` 忽略，说明、脚本和依赖清单可入库。本轮只提供外部参考，没有新增 YMGUI 应用。

## 第 88 轮：补充 Ardour 源码构建与双轨体验工程

- 背景：用户希望继续体验 Ardour。将官方 9.8 tag（`22ed8656c2533e325322ff11831448e5123e0d4b`）下载到 `参考/ardour/`，使用 Python 3 / Waf 和 GCC 11 优化构建，安装到 `参考/install/ardour/`；编入 ALSA / PulseAudio / JACK、LV2 / Linux VST / VST3 支持。
- 新增 `build-ardour.sh`、`run-ardour.sh` 和 Ubuntu 22.04 补充依赖清单；统一构建入口增加 `ardour`，`all` 依次构建三款。开发包继续按 APT 元数据校验并解包到私有目录，没有安装系统包。依赖准备器支持额外清单，修正 pkg-config 中硬编码的 include/lib 路径，解决 harfbuzz / raptor 头文件探测问题；LMMS / Hydrogen 版本命令仍正常。
- 处理 Waf 最后生成启动脚本时的 Latin-1 编码错误：维护独立 `ardour-utf8-subst.patch`，仅将 4 个 wscript 的 11 处模板替换指定为 UTF-8，构建脚本自动检查并幂等应用。未改动上游 GUI 或音频实现。源码、依赖、安装和演示产物继续忽略，补丁与说明、脚本、清单可入库。
- 启动器隔离 XDG 配置/缓存/数据；本机创建 `Reference-Demo`，导入 Hydrogen 鼓组及 LMMS 前 25 秒片段作为两条立体声轨。素材先降低音量再导入，Ardour 自动从 44.1 kHz 重采样到工程的 48 kHz。默认启动器打开已保存工程，新机器无工程时进入正常新建流程。
- 验证：1901 项 Waf 构建任务及本地安装完成，版本输出 `Ardour9.8.0`。最终构建实际启动、保存重开、播放/停止均成功，截图确认波形、播放位置与轨道/主总线电平；使用 ALSA 48 kHz。命令行从同一工程导出约 25.32 秒、48 kHz / 16 位 / 双声道 WAV，采样非静音。脚本语法、仓库自检与 `git diff --check` 通过；没有运行上游完整测试套件，也未修改 YMGUI 核心或新增应用。

## 第 89 轮：中文音乐工坊与结构化音乐制作流程

- 背景：用户确认以 Ardour 的工作区布局和 Hydrogen 的鼓机操作为参考，要求项目放在 project_Demo 并使用中文界面。新增 `project_Demo/music_studio`，应用名“音乐工坊”，按 model / audio / app / ui 分层；参考 video_stidio 的工作台、中文字库、文件系统适配和确认流程。界面、合成音色与演示为本应用实现，未复制上游 Qt/GTK 代码或鼓组资源。
- 工程模型采用 96 PPQ 和 48 kHz 立体声，提供鼓轨、内部音符乐器轨、音频轨；多轨时间线包含素材拖入、同类跨轨移动、两端裁剪、分割、重复、复制粘贴、删除、吸附、缩放、横移、轨道命名与调序。节奏型共享引用，支持独立副本；32 步模型快照历史，拖动取消恢复原状态。
- 鼓机提供 8 种内置鼓声、16/32 步节奏、逐步力度及逐鼓音量、声像、静音、独奏、试听和短音频采样替换。钢琴卷帘支持添加、移动、调整长度/力度、量化、右键删除与八度切换，配 3 种基础合成音色。默认“午后律动”包含鼓组、旋律和合成氛围音频，打开即可播放。
- SDL 队列播放与 WAV 导出复用同一混音函数；按绝对采样位置调度，支持轨道/主音量、声像、静音/独奏、电平、片段增益及淡入淡出。提供循环、当前片段试听、播放指针、速度、拍号和节拍器；循环边界持续填充队列，按实际排队数据计算可闻位置。无声卡时明确提示，不伪造播放状态。
- FFmpeg CLI 通过 argv 子进程后台导入音频，不拼 shell 命令；后台导出立体声 WAV，任务期间冻结编辑，主线程等待完成后接管结果。`.ymmusic` 以 `YMUSIC01` 文件头显式编码字段及 PCM，加载校验候选状态后替换，保存和导出使用同目录临时文件原子提交；包含覆盖/丢弃确认及每分钟自动恢复副本。
- 为满足全中文文件操作，FileDialog 新增可选 `SetTranslator`，按实例翻译按钮和屏幕状态；默认英文行为、路径/文件名及 `GetStatus` 原始状态键保持不变。新增模式切换、错误状态、路径保持和解除翻译器的回归；同步 API、AI 指南与当前状态，现有 releases SDK 未重新生成。
- 验证：RGB565 / RGB888 独立工程均完成构建和 2/2 CTest，覆盖模型与 PCM 往返、音乐时钟、分块/分割混音一致性、静音/独奏/声像/淡入、坏文件保护、真实 WAV 导入导出、鼓机/钢琴/混音/拖放/取消、循环队列及后台导入后的工程保存重开。AddressSanitizer / UndefinedBehaviorSanitizer 与泄漏检查通过；LeakSanitizer 的进程检查受沙箱限制，改在沙箱外执行本地测试后通过。根工程两种色深的 FileDialog 回归均通过。实际桌面中文窗口完成播放/暂停；内置工程导出约 8.727 秒、48 kHz / 16 位 / 双声道 WAV，峰值 -5.7 dB、均值 -24.0 dB，非静音。鼓机、钢琴和混音器截图已保存，仓库自检通过。
- 当前边界：Linux / RGB565 / RGB888、固定 1440×940；8 轨、16 个节奏型/旋律段、128 片段、每段 128 音符、16 素材、编排 1024 拍。单项音频 120 秒、总计 10 分钟，鼓声替换最长 5 秒；音频全量驻留内存，保存/打开大工程可能短暂停顿，文件任务暂无取消。录音、外部 MIDI、SoundFont、效果器/插件、自动化及时间拉伸留待后续。完整应用数增为 13，RGB888 支持 12 个，根 CTest 仍为 35；构建和截图脚本自动枚举新应用，未重跑所有旧应用。

## 第 90 轮：旋律独立试听与八种中文合成音色

- 背景：用户反馈旋律片段试听入口不明显、乐器选择偏少，并明确 Ardour 是界面功能与结构化移植的主要参考；Hydrogen 用于鼓机，video_stidio 仅用于 YMGUI 使用接入参考。同步修正应用 README、架构说明和当前状态中的参考分工。
- 鼓机和钢琴卷帘右侧新增“试听片段 / 停止试听”，从头循环当前节奏型，无需先加入编排。预览优先使用当前同类轨道，解决旧实现总取第一条同类轨道、被其他轨道独奏或自身静音挡住的问题；只创建借用 PCM 的临时工程视图，不改写真实编排及静音/独奏。保留鼓组内部静音/独奏、当前轨道音量和声像；顶部播放模式可切回整曲。
- 将隐藏的三音色轮换改为中文 Dropdown，提供柔和钢琴、明亮电琴、合成贝斯、拨弦吉他、弦乐铺底、管风琴、长笛、电子主音。新 `audio/synth.c` 用不同分音、起音/衰减/释放包络及轻微失谐区分音色，并剔除超过奈奎斯特频率的分音。琴键、“试听音色”、编排播放和导出共享同一合成函数，修复琴键试听始终使用正弦波的问题。
- 音色属于轨道，选择可撤销并随工程保存；原 0/1/2 编号保留，扩展到 0–7，文件字段布局不变。新版读取旧工程，旧版拒绝包含新增音色的工程。下拉浮层展开时阻止应用快捷键干扰，切页或文件任务关闭浮层。停止试听仍保留片段模式，用户可在顶部明确切回整曲。
- 验证：RGB565 / RGB888 构建与 2/2 CTest 通过；新增全部音色非静音、波形差异、分块一致性、工程音色往返、非法编号拒绝及指定轨道隔离测试。界面输入注入覆盖下拉选择/撤销/重做、没有编排的新轨试听、停止清队列、单音试听和整曲切换。ASan / UBSan 与泄漏检查通过；实际桌面展开中文列表、播放片段，确认播放指针和停止试听状态。更新钢琴与鼓机截图，增加音色列表截图；仓库自检通过。
- 边界：八种乐器是轻量合成音色，不是真实采样音源；未接入 SoundFont、VST/LV2 或外部 MIDI。未新增第三方依赖、YMGUI 核心 API 或根测试数量，未重打包 releases SDK。

## 第 91 轮：交响类合成音色与山海序章小样

- 背景：用户希望增加适合恢宏编曲的乐器，确认补充圆号、铜管合奏、定音鼓和合唱。音色编号追加为 8–11，总计 12 种，原八种编号和工程字段布局保持不变；中文下拉选择、琴键/片段试听、保存和撤销直接复用既有流程。
- 圆号用渐入高次分音模拟吹奏展开；铜管合奏使用轻微失谐的声部和较亮谐波；定音鼓包含鼓膜分音、短音高下滑、确定性槌击瞬态及衰减；合唱以固定共振峰和失谐分音模拟“啊”元音。四种均在共享音源路径实现，播放与离线导出一致。定音鼓放在有音高的乐器轨，建议用低八度、较长音符试听。
- 新增可选 `music_epic_demo` 构建目标及 `examples/epic_demo.c`，通过模型 API 生成“山海序章”：90 BPM、六小节、五轨（弦乐/圆号/铜管/定音鼓/合唱），15 个可编辑旋律段。输出工程与 WAV 到用户指定路径，本机产物在 RGB565 应用的 build/user 下，默认启动示例仍为“午后律动”。已打开小样桌面窗口并更新音色列表、小样截图与说明。
- 验证：RGB565 / RGB888 各 2/2 CTest 通过，全部 12 种音色覆盖非静音、波形差异、分块一致性、保存重开与试听隔离，新增低/高/最高音域有限值检查，界面覆盖最后一项合唱选择与撤销。ASan / UBSan 和泄漏检查 2/2 通过。小样重新加载后导出的 WAV 与生成器输出逐字节一致；48 kHz / 16 位 / 立体声、16 秒、峰值 -7.4 dB、均值 -22.3 dB，未削波。仓库自检和 diff 空白检查通过。
- 边界：这些是轻量合成音色及合成编曲示例，不是真实交响乐采样；本轮没有引入 SoundFont、外部乐器插件、混响或录音。无新增第三方依赖及 YMGUI 核心接口，根工程仍为 35 项测试。


## 第 92 轮：片段选择、拖动反馈与全部音源淡化

- 背景：用户反馈从素材库选旋律后，钢琴卷帘变化但轨道属性与音色未跟随；拖动素材无反馈，单段试听听不出淡化。素材库选择现按当前准确引用、当前轨道引用、其他实际引用顺序解析目标，未编排素材清除旧片段选择并保持同类轨道。时间线跨轨移动也同步属性；选择不写入历史。
- 素材移动超过 6 像素显示跟随卡片，时间线显示吸附落点矩形，绿色可放、红色不匹配；释放重新验证才加入编排，取消/失焦隐藏预览且不写模型。共享旋律仍归轨道音色控制，没有给共享节奏型增加会冲突的音色字段。
- 片段淡化改为 0–10 秒中文输入框，回车确认、0 关闭、非法值不产生历史；无选中片段时提示先选编排片段，时间线上显示淡化斜线。原混音包络本已覆盖音源，实际缺口是试听临时工程丢失片段参数；现保留裁剪、长度、增益、淡入淡出，音频片段也可单独试听。音源单音按钮改名“原声试听”，与编排片段效果区分。
- 验证：逐样本验证全部 21 种音色、九种内置鼓、自定义鼓采样和导入 PCM 的淡入淡出乘法包络；所选片段试听与整曲相应部分逐字节相同。输入注入覆盖引用轨道及属性同步、共享片段、拖动跟随/有效无效目标/取消、淡化输入及音频独立试听。RGB565 / RGB888 应用各 2/2 CTest 通过；截图更新到当前大工作区。音源扩展和存储兼容见下一轮。

## 第 93 轮：真实采样基准、特征再合成、响指及更大工作区

- 背景：原“合唱人声”实为电子元音，用户还反馈拍手不准、希望增加响指，并要求用真实采样提取特征改善合成音色。保留原 0–11 编号及声音，11 改名“合成元音”；新增合唱采样 12、钢琴/弦乐/铜管/长笛采样 13–16，以及从后四类采样分析的特征合成 17–20。新建乐器轨默认采样钢琴，读取旧工程不重写其音色编号或轨道名称。
- 资源使用 GeneralUser GS 2.0.3 官方固定提交，放在 extern_lib/GeneralUser-GS 并保留原许可、来源、原 README、校验及重制工具。完整 32 MB SF2 仅本地准备、被忽略；五个专用 bank 共约 18.7 MB，解码 PCM 约 37.4 MB，应用不链接 FluidSynth 或 Python。每类乐器 13 个音区（MIDI 36–84、间隔 4 半音）、固定力度 100、24 kHz 单声道；钢琴自然衰减，其余采样保留起音并交叉淡化循环。缺失或格式不符明确失败，不静默回退合成。
- 离线工具用 NumPy 对四类乐器各音区的 18 个时间点做 4096 点 Hann 窗 FFT，积分前 24 个谐波主瓣能量；生成约 90 KB 编译后振幅表与分析 JSON。特征版使用时变振幅、固定相位振荡器重建，不播放 PCM，保留部分频谱与衰减特征。没有建模完整瞬态/气流噪声、合奏相位、颤音和多力度层，明确它是可比较、继续调音的近似，未声称完全还原乐器。
- 拍手从同一资源的 Hand Clap 样本直接提取成 48 kHz 单声道 WAV，替换原周期门控噪声；旧工程的内置拍手随之改变，自定义采样保留。新增第九行“响指”，用短宽带击打、掌腔共鸣及快速衰减合成，可独立编排、试听、调力度/声像、替换采样。存储升为 YMUSIC02 九鼓布局，仍按 YMUSIC01 八鼓布局读取旧工程，响指初始化为空；新版本保存的工程需新版本打开。
- 工作区扩为 1680×1050，时间线与编辑网格加宽，九行鼓声每行 36 像素、琴键 31 像素；混音器列加宽并显示当前音源。音色分为原有音色、乐器采样、特征合成；沿用核心现有 Dropdown，切换采样/特征时保留对应乐器，撤销/加载从音色编号恢复分类。没有更改核心 Dropdown 上限或公共 API。
- 新增可选 music_sound_demo，生成可编辑的八轨“采样与特征对照”、16 秒总 WAV、四段各 4 秒的乐器采样/特征对照及 4 秒拍手/响指对照；产物在 build 下专用 user/音源对照目录。另保留旧/新合唱对照与采样合唱版山海序章，不覆盖用户旧小样。
- 验证：RGB565 / RGB888 各 2/2 CTest，覆盖 21 音色非静音/差异/分块/音域/工程往返，全部音源淡化，九鼓非静音与响指短瞬态尾部，独立编码的旧八鼓夹具读取及九鼓保存重开，分类选择与撤销、响指最后一行命中。消毒器发现核心 AA 画线的负斜率与负起点使用左移负数，改为 64 位乘法；添加负斜率和负坐标绘图回归，两色深的 test_draw / test_aa 均通过。根 CTest 数量仍为 35，未重新打包 releases SDK。
- 最终验证：ASan / UBSan / LeakSanitizer 应用 2/2 通过，RGB565 / RGB888 图元与 AA 各 2/2 通过。七张当前界面截图已更新并检查，音源总对照 16 秒、乐器与打击乐对照各 4 秒，全部非静音且未削波；采样/特征对照峰值约 -21.6 dBFS。仓库自检及 diff 空白检查通过。

## 第 94 轮：所有乐器与鼓组统一采样，实录拍手和响指

- 背景：用户反馈拍手、响指发闷，随后明确要求所有乐器均改用采样。上一版拍手是 GeneralUser GS 采样，响指仍是合成；旧响指测得首 250 ms 的频谱能量约 51% 位于 1 kHz 以下。按新范围移除加法合成器、特征再合成器、特征表及分类控件，所有音色统一到 PCM 播放。
- 乐器保留 13 种不同身份：钢琴、电钢琴、贝斯、吉他、弦乐、管风琴、长笛、电子主音、圆号、铜管、定音鼓、人声元音、合唱。GeneralUser GS 的完整固定来源和许可保留，离线制作 13 个多音区 bank，约 48.7 MB 文件 / 97.3 MB 解码内存；电子主音是电子音色的采样，不声称为原声乐器现场实录。击弦/拨弦/定音鼓自然衰减，其余保留起音并交叉循环；缺失资源不回退合成。正弦表只用于节拍器，默认和弦铺底也从管风琴 PCM 渲染。
- 鼓组九声部全部使用 WAV。七种鼓声从同一 SoundFont 的标准鼓组制作；拍手换为 VCSL 的 SoloClap_vl3 实录（固定提交 c1ea7bcc3c7309650ab0da9d15c9cd1fbc4a4c7e），响指换为 Vinrax 在 OpenGameArt 发布的 snap_of_the_fingers3 实录，两者 CC0。新建 extern_lib/BodyPercussion，保留原文件、来源、SHA、许可和重制脚本。预处理采用 650 Hz 高通、2.6 kHz 高频搁架 +6 dB、起音前空白裁剪、0.5 ms 预卷和短尾淡出；播放无需 FFmpeg 或 Python，不叠加合成瞬态。
- 实录拍手和响指成品约 163 ms / 96 ms，标准试听混音峰值约 -14.3 dBFS，未削波；响指首 250 ms 的 1 kHz 以下能量约 0.46%，3 kHz 以上约 46.7%。这些是检查数据，不把频谱比例等同于听感拟真度；另生成 RMS 音量匹配的前后对照，防止只靠增大音量比较。旧试听输出保留，新文件放 build/user/全采样试听及打击乐调整目录。
- 旧工程的 0–12 继续识别，但按用户要求改用采样声音；上一版 13–16 和 17–20 的重复类别读取时映射为钢琴 0、弦乐 4、铜管 9、长笛 6。导入素材、自定义鼓声和编排参数不变，用户轨道名称不自动改写；存储仍为 YMUSIC02，继续兼容八鼓 YMUSIC01。新增逐个旧编号的字段迁移测试，未知编号仍拒绝。
- 长吊镲资源为 3.5 秒，混音器回看旧节奏周期改为使用实际资源最长长度，避免原 1.2 秒合成鼓上限漏掉尾音。快节奏重复节奏与展开节奏逐样本一致。统一音色 Dropdown 只列 13 个采样项，右侧显示“全部使用采样音源”，无特征合成入口。可选 music_sound_demo 改为输出 8+5 轨的两组全采样乐器试听和拍手/响指试听，music_epic_demo 全部使用采样。
- 验证：RGB565 / RGB888 应用各 2/2 CTest 通过，覆盖全部音色与鼓声非静音、分块、自然结束、循环、淡化、试听隔离、旧编号迁移、工程往返和 UI 选择撤销；ASan / UBSan / LeakSanitizer 2/2 通过。资源格式与 SHA 清单检查通过，文档同步来源、资源体积、映射和能力边界。此次无 YMGUI 公共 API 改动，根测试数量仍为 35。


## 第 95 轮：合唱实时试听断续与音频供给线程

- 背景：用户反馈合唱片段实时试听出现断续噪声，离线导出正常。原实现每次 GUI 主循环仅补约 43 毫秒音频，之后刷新大面积时间线/钢琴卷帘；真实桌面窗口配合 SDL dummy 音频时，30 次刷新测得最长 48 毫秒、1 次队列耗空，另停顿主线程 300 毫秒后队列归零，可闻位置只推进剩余 2048 帧。离线导出没有供音时限，因此相同混音内容正常。
- 实时整曲与片段试听改由独立 music-audio 线程持续补队列，目标 4 × 1024 帧、约 85 毫秒，每约 2 毫秒检查。启动先暂停设备并预填，再开始消费；单音和鼓声试听先完整入队再播放。保留同一采样资源、混音规则、片段效果及离线导出，不为掩盖欠载修改合唱采样。
- 工作线程持有工程值快照，借用只读 PCM，不访问 YMGUI 或原模型。共享游标/电平/完成状态用 mutex，停止用原子标志并等待线程退出；编辑和定位清队列重建快照，打开工程调整为先收尾音频线程再加载，避免加载成功释放旧 PCM 时后台仍引用。加载失败保留原工程并同步停止状态，新建和退出沿用先收尾后释放顺序。
- 验证：RGB565 / RGB888 各 2/2 CTest，ASan / UBSan / LeakSanitizer 2/2 通过。增加合唱试听下连续重绘并停顿主线程超过半秒的供音检查、快照隔离/编辑发布、非循环尾块自然结束，以及播放导入 PCM 时加载失败/成功的资源生命周期回归。修复后同一桌面探针测得刷新最长 66 毫秒，30 次刷新无队列耗空；主线程再停 300 毫秒后队列仍为 4096 帧，可闻位置推进 14336 帧。计时是本机一次观测，不作为固定性能承诺。
- 边界：队列余量不包含声卡/音频服务延迟；未增加录音或低延迟实时演奏能力，系统整体过载仍可能欠载。本轮没有变更音色、工程格式、YMGUI 公共接口或根测试数量。


## 第 96 轮：可编辑曲库第一辑与 MIDI 离线转换

- 背景：用户希望有现成曲库，转换成音乐工坊工程后打开试听。选择 Mutopia 官方的五份 Public Domain 排版/MIDI：致爱丽丝、土耳其进行曲、G大调小步舞曲、绿袖子和欢乐颂 SATB 主题编配。原 MIDI、LilyPond 源文件/压缩包、RDF 许可元数据及 URL/SHA-256 清单保留在应用 library/sources；没有下载第三方录音。
- 生成七个原生 YMUSIC02 工程，时长 24–48.5 秒：两个钢琴主题节选、小步舞曲钢琴全 MIDI/长笛弦乐前半段、绿袖子吉他完整 MIDI、欢乐颂完整主题编配的合唱版/铜管圆号版。音符可编辑，全部使用现有采样音源；工程无 PCM、每份 8–14 个乐句，保持现有容量限制。文件名和曲库说明区分全曲、节选及改配器，不声称欢乐颂主题为整部交响曲。
- 增加 Python 标准库离线转换工具：解析这批 MIDI 格式 0/1 的 PPQ、音符、running status 与元事件；按清单选择范围/轨道/配器/固定速度，舍入到 96 PPQ。按完整音符装箱，允许片段重叠以保留跨乐句延音，复用相同乐句并逐项核对展开音符；超容量、过长单音及损坏输入报错。写显式 YMUSIC02 字段及尾标记，已有工程内容不同则拒绝覆盖。该工具不构成应用内通用 MIDI 导入功能。
- 取舍：部分钢琴双手合轨以容纳完整短曲；较长曲目明确节选。原控制器/踏板、程序音色及速度图不导入，检测信息写入报告；致爱丽丝的 3/8 在当前以四分音符为拍的界面中按 tick/BPM 同乘二显示为 3/4，保持实际音长；弱起没有额外小节标记。新增曲库说明和应用入口文档。
- 验证：六项 Python 转换回归通过，包含 running status、零力度 note-off、坏数据、跨乐句延音、复用/容量限制、精确重制、保护已有编辑和时值缩放。七份工程均经实际 C 加载器校验、保存重开及模型比较，完整音符未丢失；通过应用逐个导出为 48 kHz / 16 位 / 立体声 WAV，均非静音、无削波，峰值约 -20.0 至 -10.9 dBFS。试听 WAV 位于 build/rgb565/project_Demo/music_studio/user/曲库试听；源码资源与小体积原生工程在 library 下可入库。七份工程的三帧无头 GUI 启动均通过，并检查合唱工程的时间线/钢琴卷帘截图；已实际启动《欢乐颂》合唱工程的 build/user 副本供用户试听，保留已有窗口。该目录另含可编辑副本及曲库压缩包。本轮不改音频引擎、工程容量、核心 API 或 CTest 数量。


## 第 97 轮：钢琴卷帘细分步长与短音符编辑

- 背景：用户发现曲库中的短音符能播放，但手动创建及拖动右端必须占满一格。模型已有最小 1 tick（1/96 拍）能力，缺口是钢琴编辑硬编码 MS_STEP=24，以及极短音符绘制宽度被扣除边距后消失。
- 钢琴工具栏右侧增加步长下拉框：1/16、1/32、1/64、1/8 三连音、1/16 三连音、1/32 三连音、自由模式，对应 24/12/6/32/16/8/1 tick。选择设置新音符长度和编辑步长，不修改已有音符、不写工程历史；会话默认仍为十六分步长，不修改工程文件格式。顶部吸附开关接入钢琴，关闭时按 1 tick 细调；鼓机和时间线原有规则保持。
- 创建、拖动、音长加减与量化统一使用有效步长。拖动采用相对增量，保留已有非网格起点，长度最小 1 tick；音长加减从所选音符实际长度开始。量化只吸附起点，边界钳制保持原长度。显示改为拍数，细分网格随选择变化；极短音符至少 3 像素，绘制/命中统一，右端手柄自适应宽度，支持点击、移动、拉长和取消恢复。
- 两个下拉弹层共用快捷键阻止规则，切换页签或文件任务关闭步长弹层。说明文档增加入口、时值与会话边界，新增细分网格截图并人工检查菜单、音符和布局。
- 验证：RGB565 / RGB888 各 2/2 CTest 通过，ASan / UBSan 2/2 通过（本轮沙箱内关闭泄漏探测）。输入注入覆盖全部七项选择/创建、选择不写历史、短音符不被拉长、缩放/撤销/重做、自由细调、最短音符在 32 步视图的命中/缩放/取消、三连音量化、乐句边界和弹层收尾。检查中既有音频快照回归曾因电平采样落在示例旋律休止处失败，测试改用连续长音并在结束后恢复原旋律，避免休止被误判为供音故障；未改音频实现。文档及仓库检查通过，核心 API 和测试数量不变。


## 第 98 轮：曲库转换脚本使用与维护文档

- 背景：用户确认保留转换脚本，并要求记录相关文档。脚本已在 tools/build_library.py；本轮核对现有实现后补齐说明，不重复生成或替换曲库工程。
- 曲库 README 明确脚本、固定配置与来源目录、默认输出及 --output 的含义、离线标准库依赖、同名工程保护和批量中途失败行为。补齐 catalog 字段表、源 MIDI 轨道编号与通道的区别、0–12 音色编号、速度/拍号时间缩放、来源 SHA 清单以及新增曲目验证流程。
- 说明脚本仅输出 .ymmusic 和 conversion.json，WAV、可编辑用户副本及 ZIP 是另行生成的本机 build 产物；脚本不是应用内通用 MIDI 导入。同步应用 README、架构文档、仓库 AI 指南与当前状态，记录序列化格式变更时同步检查转换器的维护约束。
- 验证：文档入口与实现逐项核对；仓库自检（Markdown 本地链接、资源及数量检查）与 git diff --check 通过。本轮只修改文档，未改运行时代码、工程格式、曲库资源或测试数量，也未重复执行此前已通过的音频回归。


## 第 98 轮：钢琴卷帘框选开关与批量音符编辑

- 背景：用户希望在空白处拖动框选已有音符，避免点击就新增。本轮仅扩展音乐工坊钢琴编辑，不改时间线/鼓机的放置方式和工程格式。
- 在卷帘网格右上加入“框选已有：开/关”和“删除所选”。开启后空白处只选择，拖动显示矩形并实时高亮、计数，单击空白清除选择；双向矩形按可见音符外形交叠判定，最短 3 像素音符可选，不误选其他八度。点击未选音符改为单选，点选组内音符保留整组；关闭恢复原来的点击放置。
- 已选音符可一起移动或拖右端改变长度，使用手势前快照和统一边界限制，保留相对起点、音高及长度差。音长按钮、力度滑块和量化作用于选区，删除按原下标筛除并一次写入历史；Delete 在卷帘和框选工具栏内删除音符，时间线保持片段删除。组编辑可撤销/重做；拖动取消恢复模型，取消框选恢复之前的选择。
- 选择为会话状态：开关、框选、点选不标脏、不写工程历史，也不重启音频供给。切换片段/离开卷帘、加载/新建、历史恢复和可能重新排列音符下标的操作清理选择；手势进行中阻止会改变上下文的应用命令。同步使用说明、架构与当前状态，增加真实界面框选截图并检查按钮、提示和矩形显示。
- 验证：RGB565 / RGB888 各 2/2 CTest，ASan / UBSan 2/2 通过（沙箱内关闭泄漏探测）。新增输入注入覆盖开关不标脏、空白点击不新增、正反向框选及最短音符、隐藏八度排除、取消恢复、保持组内间距和左边界、批量拖改/按钮音长/力度/量化、键盘和按钮删除、撤销重做、空选区无操作、切换片段清理与关闭后恢复放置；仓库自检通过。核心公共 API 和 CTest 数量不变。


## 第 99 轮：上下轨道的 Ctrl＋滚轮横向视图缩放

- 背景：用户明确希望放大音符/片段的显示宽度，便于操作短音符。将 Ctrl＋滚轮接入鼠标所在的上方编排区或下方钢琴/鼓机，两区独立缩放，实际音乐时值不变。
- 编排区沿用 zoom/scroll，滚轮以鼠标指向的 tick 为锚点，范围 0.12–12 像素/tick；原放大/缩小按钮复用同一逻辑，以左端为锚点。下方增加 1–16 倍视图缩放及浮点 tick 偏移，Shift＋滚轮或横向滚轮用于平移；缩小到 1 倍恢复整段显示。视口统一钳制，时间线整数 tick 偏移的锚点误差最多约半 tick。
- 下方网格绘制、最小宽度、命中、创建、右端拉长、整组移动、框选、播放游标、鼓点和力度柱共享坐标变换。键盘/鼓名保持固定，网格绘制裁剪在右侧；应用矩形先按可见范围裁剪再转 GYcoord，避免大时间坐标/宽度在 16 位坐标下回绕。视图参数仅在会话内保存，不改模型、脏标记、撤销历史或音频供给快照；拖动和文件任务期间不改视口。
- Ctrl/Shift 通过应用输入过滤器维护，按下和抬起在忙碌/拖动状态下仍同步；SDL 失焦沿用已有修饰键取消事件。未按修饰键时，上方滚轮仍横移，下方保留八度/鼓点力度操作。同步应用 README、架构和当前状态，加入上下区域放大后的真实截图并检查短音符、裁剪和提示。
- 验证：RGB565 / RGB888 各 2/2 CTest、ASan / UBSan 2/2 通过（沙箱内关闭泄漏探测）。新增输入注入覆盖鼠标锚点、上下视口独立、工程数据/历史/脏标记不变、上下限、Shift/横向滚轮平移、普通滚轮兼容、放大后的短音符命中/拉长/创建/框选/整组移动与撤销、鼓点编辑以及拖动中禁止缩放并正确接收修饰键抬起。仓库自检和 diff 空白检查通过；核心 API、工程格式和 CTest 数量不变。


### 第 99 轮文档补充：编辑操作指南

- 按用户要求整理独立的音乐工坊编辑操作指南，提供放大短音符后编辑的步骤、上下区域滚轮速查、框选批量操作、七种步长时值和常用快捷键；核对并写明量化作用范围、共享旋律引用、视图设置不入工程及音符选区暂无复制粘贴入口。
- 应用 README、架构文档、仓库 AI 指南和当前状态增加入口及维护约束，沿用已验证的缩放/框选截图。说明与 `ui/editors.c`、`ui/timeline.c`、`app/actions.c` 和 `main.c` 的当前行为核对。
- 本次仅修改文档，不改变运行时代码、工程格式或测试数量。仓库自检与 diff 空白检查通过；未重复运行已通过的构建与音频回归。


## 第 100 轮：README 许可入口与新应用展示图

- 背景：核对仓库当前协议为 Apache-2.0，补齐 README 原先缺少的协议徽章和独立许可入口；用户明确此次展示更新指新 demo 的运行效果图，并要求每个 demo 只展示一张。
- 新增随仓库保存的 SVG 协议徽章，点击跳转根 LICENSE；补充简短开源许可说明及第三方依赖、音源、曲库和参考软件的独立许可入口，同步当前状态。未改变许可证原文或授权范围。
- 从当前 RGB565 构建重新截取音乐工坊鼓机、钢琴与混音界面并检查，根 README 采用一张“午后律动”鼓机与多轨时间线全景代表图；不添加多图画廊。更新展示文案与当前状态，每个 demo 一图，详细交互截图仍留在应用专题文档。
- 验证：RGB565 音乐工坊构建和有限帧截图运行成功，检查三张 1680×1050 实际渲染图；SVG XML 结构及文本、仓库文档链接自检和 git diff --check 通过。本轮未修改运行时代码，未重复运行无关音频回归。

### 提交范围补充

- 按用户要求，根 `.gitignore` 排除整个 `参考/` 和新增 WAV 音频，文件保留本地；13 个乐器 bank、曲库工程、来源许可、校验清单及重制脚本仍提交。FFmpeg 已有两个 `.wav` 后缀文件实际为文本测试基准，未修改。
- 移除已排除参考目录的 Markdown 链接，文档检查跳过整个本地参考目录；更新当前状态和音源/应用说明，明确新检出后必须先准备鼓声 WAV 才能构建。此约定取代第 87 轮中参考说明与脚本可入库的边界。

### 采样提交范围修正

- 用户进一步明确 `extern_lib/` 内 WAV 为运行必需资源，应随仓库提供；在根忽略规则中增加该目录 WAV 的例外，提交七种 GeneralUser GS 鼓声以及拍手/响指原始与处理后录音，共 11 个 WAV。
- 同步修正 README、当前状态及音源文档：正常构建使用已附带采样，无需重制。其他位置的 WAV 和整个 `参考/` 继续排除。本条取代上一条对所有新增 WAV 的排除约定。

### 原始 MIDI 提交范围调整

- 按用户要求，将五份原始 MIDI 从 Git 跟踪中移除并增加 `.mid` / `.midi`（兼容大小写）忽略规则，文件保留本机；七份转换好的 `.ymmusic` 工程继续保留在应用曲库。
- 保留来源 URL、SHA-256、乐谱与许可元数据、转换脚本和配置。同步曲库说明、应用入口、架构和当前状态，明确播放不需要原始 MIDI，重制及完整 Python 转换回归需要先按清单恢复。
